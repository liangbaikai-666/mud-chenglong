# 模块3 C++ 类设计（新架构版）

基于 `game_api.h`抽象基类架构，以下是模块3（状态效果系统）的C++类设计。**返回值与实现思路写在注释中，不提供具体实现，由组员自行编写。**

cpp

cpp

下载

复制

```
#ifndef STATUS_EFFECT_MANAGER_IMPL_H
#define STATUS_EFFECT_MANAGER_IMPL_H

#include "game_api.h"       // 包含 IStatusEffectManager、StatusID、TalismanID 等
#include <map>
#include <vector>
#include <string>

// ==================== 状态效果配置结构体（模块3内部使用） ====================
struct StatusConfig {
    StatusID id;                    // 状态ID
    std::string name;               // 状态名称
    int default_duration;           // 默认持续回合数
    int max_stack;                  // 最大叠加层数（0=不可叠加，1=可刷新持续时间）
    // 效果参数
    int strength_modifier;          // 力量修正值（可正可负）
    float hit_rate_modifier;        // 命中率修正（乘数，如0.8=80%）
    int magic_damage_modifier;      // 魔法伤害修正
    int max_hp_modifier;            // 最大HP修正
};

// ==================== StatusEffectManager 类（继承 IStatusEffectManager） ====================
class StatusEffectManager : public IStatusEffectManager {
private:
    // ---- 核心数据 ----
    std::map<StatusID, int> active_statuses;        // 当前激活的状态及其剩余回合数
    std::vector<StatusConfig> status_configs;       // 状态配置表

    // ---- 跨模块指针（通过抽象基类，由外部注入） ----
    ITalismanManager* talisman_mgr;      // 符咒管理器指针
    IBattleEngine* battle_mgr;           // 战斗引擎指针

public:
    // ---- 构造函数 ----
    /**
     * 实现思路：初始化 active_statuses 为空，填充 status_configs 配置表
     *          （MONKEY=1 虚弱, RESIST_UP=2 抵抗提升等，含修正参数）
     */
    StatusEffectManager();

    // ==================== IStatusEffectManager 接口实现 ====================
    /**
     * 添加状态效果
     * @param target 目标实体
     * @param id 状态ID（StatusID enum class）
     * @param duration 持续回合数（0=使用配置表默认值）
     * @return void
     * 实现思路：若 duration==0，从 status_configs 查找默认持续时间；
     *          若状态可叠加（max_stack>1），增加层数；否则刷新持续时间。
     *          将状态加入 active_statuses，调用内部修正方法影响 target 属性
     */
    void add_status(Entity* target, StatusID id, int duration = 0) override;

    /**
     * 移除状态效果
     * @param target 目标实体
     * @param id 状态ID
     * @return void
     * 实现思路：从 active_statuses 中移除该状态，调用内部修正方法恢复 target 属性
     */
    void remove_status(Entity* target, StatusID id) override;

    /**
     * 查询目标是否持有指定状态
     * @param target 目标实体
     * @param id 状态ID
     * @return bool true=持有，false=未持有
     * 实现思路：return active_statuses.count(id) > 0;
     */
    bool has_status(Entity* target, StatusID id) const override;

    /**
     * 获取状态剩余持续回合数
     * @param target 目标实体
     * @param id 状态ID
     * @return int 剩余回合数（0=未持有或已过期）
     * 实现思路：查找 active_statuses[id]，返回对应值
     */
    int get_status_duration(Entity* target, StatusID id) const override;

    /**
     * 回合结束时调用（由战斗引擎驱动）
     * @return void
     * 实现思路：遍历 active_statuses，所有 value > 0 的减1；
     *          若减至0则移除对应状态并恢复属性修正
     */
    void on_turn_end() override;

    /**
     * 战斗开始时调用
     * @return void
     * 实现思路：清空 active_statuses，重置所有临时状态
     */
    void on_battle_start() override;

    /**
     * 战斗结束时调用
     * @return void
     * 实现思路：清空 active_statuses，移除所有战斗中的临时状态效果
     */
    void on_battle_end() override;

    /**
     * 符咒效果请求回调（由符咒模块调用）
     * @param caster 施放者
     * @param talisman_id 符咒ID
     * @return void
     * 实现思路：根据 talisman_id 判断是否需要施加/移除状态
     *          （如蛇符咒触发中毒状态），调用 add_status/remove_status 处理
     */
    void on_talisman_effect_request(Entity* caster, TalismanID talisman_id) override;

    /**
     * 施加剧情状态（由关卡/剧情系统调用）
     * @param target 目标实体
     * @param id 状态ID
     * @param duration 持续回合数
     * @return void
     * 实现思路：直接调用 add_status(target, id, duration)，不受常规叠加规则限制
     */
    void apply_story_status(Entity* target, StatusID id, int duration) override;

    // ==================== 跨模块指针注入（内部使用，非接口） ====================
    /**
     * 设置符咒管理器指针
     * @param mgr ITalismanManager 实现类指针
     * @return void
     */
    void set_talisman_manager(ITalismanManager* mgr);

    /**
     * 设置战斗引擎指针
     * @param mgr IBattleEngine 实现类指针
     * @return void
     */
    void set_battle_engine(IBattleEngine* mgr);

    // ==================== 模块3内部方法（不放入接口） ====================
    /**
     * 修正目标力量属性
     * @param target 目标实体
     * @param modifier 修正值
     * @return void
     * 实现思路：调用 target->modify_attr("strength", modifier, "add")
     */
    void modify_strength(Entity* target, int modifier);

    /**
     * 修正目标命中率
     * @param target 目标实体
     * @param modifier 修正乘数（如0.8表示降至80%）
     * @return void
     * 实现思路：通过内部命中率计算逻辑调整 target 的命中率属性
     */
    void modify_hit_rate(Entity* target, float modifier);

    /**
     * 修正目标魔法伤害
     * @param target 目标实体
     * @param modifier 修正值
     * @return void
     * 实现思路：调整 target 的魔法伤害加成属性
     */
    void modify_magic_damage(Entity* target, int modifier);

    /**
     * 修正目标最大HP
     * @param target 目标实体
     * @param modifier 修正值
     * @return void
     * 实现思路：调用 target->modify_attr("health", modifier, "add")，并同步调整 current_hp
     */
    void modify_max_hp(Entity* target, int modifier);

    /**
     * 获取所有激活状态（供调试/UI使用）
     * @return const std::map<StatusID, int>& 激活状态映射的引用
     */
    const std::map<StatusID, int>& get_active_statuses() const;
};

#endif // STATUS_EFFECT_MANAGER_IMPL_H
```

------

**说明：**

- 以上仅为 `.h`头文件框架，所有方法只声明、注释返回值意义与实现思路，不含 .cpp 实现代码
- 组员根据注释中的"实现思路"自行编写 `.cpp`文件
- 跨模块调用全部通过抽象基类指针（`ITalismanManager*`/ `IBattleEngine*`）
- 状态ID全部使用 `StatusID`enum class，符咒ID使用 `TalismanID`enum class
- 头文件引用：`game_api.h``