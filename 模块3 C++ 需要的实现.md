**模块3 C++类设计**（状态效果/Buff/Debuff管理器）：

cpp

```cpp
#ifndef MODULE3_STATUS_EFFECT_H
#define MODULE3_STATUS_EFFECT_H

#include "game_api.h"  // 包含共享枚举、前置声明Entity/Player/Enemy、状态ID常量等
#include <map>
#include <string>
#include <vector>

// ==================== 状态基础数据结构（功能1） ====================
struct StatusData {
    std::string id;                  // 状态ID（string类型，如"MONKEY"、"RESIST_UP"）
    std::string name;                // 中文名，如"猴化"、"抵抗提升"
    // 注：【修补版】已移除NEUTRAL枚举值，仅保留BUFF/DEBUFF分类
    enum { BUFF, DEBUFF } type;      // 状态分类
    std::string description;         // 效果描述
    int default_duration;             // 默认持续回合（猴化固定2回合，此处填2）
    float damage_modifier;           // 伤害修正系数（如0.5表示伤害减半，1.0为无修正）
    float defense_modifier;          // 防御修正系数
    bool is_removable;               // 是否可被净化/移除
};

// ==================== StatusEffectManager 类 ====================
class StatusEffectManager {
private:
    // ---- 功能1：基础数据 ----
    std::map<std::string, StatusData> all_statuses;  // 所有状态配置数据（键=状态ID）

    // ---- 功能2：状态存储 ----
    // 键=实体指针（Player*或Enemy*），值=该实体身上的状态map（键=状态ID，值=剩余回合，-1为常驻）
    std::map<Entity*, std::map<std::string, int>> active_statuses;

public:
    /**
     * 构造函数
     * 实现思路：调用 load_all_status_data() 加载所有状态配置
     */
    StatusEffectManager() {
        load_all_status_data();
    }

    // ---- 功能1：数据加载 ----
    /**
     * 加载所有状态配置数据
     * @return void
     * 实现思路：硬编码或从配置文件读取所有状态数据存入 all_statuses map。
     *          例如：all_statuses["MONKEY"] = {"MONKEY", "猴化", DEBUFF, "...", 2, 1.0, 1.0, false};
     *          注：【修补版】猴化（MONKEY）固定2回合，不可被移除。
     */
    void load_all_status_data();

    /**
     * 获取状态数据
     * @param id 状态ID（string）
     * @return const StatusData& 对应状态的常量引用
     * 实现思路：返回 all_statuses[id]
     */
    const StatusData& get_status_data(const std::string& id) const;

    // ---- 功能2：添加/移除/查询（核心） ----
    /**
     * 添加状态到目标实体
     * @param target 目标实体指针（Entity*，Player或Enemy）
     * @param id 状态ID（string）
     * @param duration 持续回合（-1为常驻，>0为有限回合）
     * @return void
     * 实现思路：若 target 已在 active_statuses 中，直接在其子map中插入/覆盖 id->duration。
     *          调用 apply_stat_modifiers(target, id) 立即应用数值修正。
     *          【跨模块】模块5剧情强制添加状态时调用此函数。
     */
    void add_status(Entity* target, const std::string& id, int duration);

    /**
     * 移除目标实体上的指定状态
     * @param target 目标实体指针
     * @param id 状态ID（string）
     * @return void
     * 实现思路：从 active_statuses[target] 中 erase 该 id，调用 remove_stat_modifiers 移除数值修正。
     *          【跨模块】供模块2的 request_effect_removal 间接调用。
     */
    void remove_status(Entity* target, const std::string& id);

    /**
     * 检查目标是否拥有指定状态
     * @param target 目标实体指针
     * @param id 状态ID（string）
     * @return bool true=拥有该状态，false=无此状态或已过期
     * 实现思路：查找 active_statuses[target] 中是否存在 id 且对应值 > 0 或为 -1。
     *          【跨模块】模块2的 calculate_spell_damage 调用此函数检查 "RESIST_UP" 以决定伤害减半。
     */
    bool has_status(Entity* target, const std::string& id) const;

    /**
     * 获取目标指定状态的剩余回合
     * @param target 目标实体指针
     * @param id 状态ID（string）
     * @return int 剩余回合（-1=常驻，0=不存在/已过期）
     * 实现思路：返回 active_statuses[target][id] 的值
     */
    int get_status_duration(Entity* target, const std::string& id) const;

    // ---- 功能3：持续时间更新 ----
    /**
     * 回合结束更新所有状态持续时间
     * @return void
     * 实现思路：遍历 active_statuses，对所有实体的状态map中值 > 0 的条目减1；
     *          若减至0，调用 remove_status 移除该状态并清除数值修正。
     *          【跨模块】由战斗引擎（模块1）在每回合结束时调用。
     */
    void on_turn_end();

    // ---- 功能4：与符咒模块交互 ----
    /**
     * 符咒效果请求回调
     * @param talisman_id 符咒数字ID（int）
     * @param caster 施法者指针（Entity*）
     * @return void
     * 实现思路：【跨模块修补】由模块2的 trigger_talisman 在常驻符咒触发时调用。
     *          根据 talisman_id 映射到对应的状态ID（如：牛符咒→攻击Buff，蛇符咒→暴击Buff等），
     *          然后调用 add_status(caster, mapped_status_id, duration) 添加Buff。
     */
    void on_talisman_effect_request(int talisman_id, Entity* caster);

    // ---- 功能5：与关卡模块交互 ----
    /**
     * 剧情强制添加状态
     * @param id 状态ID（string）
     * @param target 目标实体指针（通常为Player*）
     * @return void
     * 实现思路：【跨模块】由模块5在剧情节点调用（如强制猴化变形）。
     *          直接调用 add_status(target, id, duration)，猴化固定传2回合。
     */
    void apply_story_status(const std::string& id, Entity* target);

    // ---- 功能6：战斗生命周期管理 ----
    /**
     * 战斗开始回调
     * @return void
     * 实现思路：清除所有实体的临时状态（保留常驻符咒产生的状态），重置战斗相关的状态追踪。
     */
    void on_battle_start();

    /**
     * 战斗结束回调
     * @return void
     * 实现思路：移除所有战斗临时状态，但保留跨战斗的常驻状态（如装备符咒的被动效果）。
     */
    void on_battle_end();

private:
    // ---- 内部辅助方法 ----
    /**
     * 应用数值修正
     * 实现思路：根据 StatusData 中的 damage_modifier / defense_modifier，修改 target 的对应属性
     */
    void apply_stat_modifiers(Entity* target, const std::string& id);

    /**
     * 移除数值修正
     * 实现思路：将 target 的对应属性恢复为修正前的原始值
     */
    void remove_stat_modifiers(Entity* target, const std::string& id);
};

#endif // MODULE3_STATUS_EFFECT_H
```