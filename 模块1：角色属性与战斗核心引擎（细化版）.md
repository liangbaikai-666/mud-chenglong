#### 模块1：角色属性与战斗核心引擎（修补版）

**共享头文件与前置声明**

- `#include "shared_attributes.h"`（属性名常量，如 `ATTR_STRENGTH`、`ATTR_HP`等）
- `#include "character_fwd.h"`（模块1自身提供，内含 `class Player;``class Enemy;`前置声明）

#### 功能1：基础属性定义与初始化

**参数（共7项，移除了 money，改由模块4管理）：**

- `strength`(int)：范围1~99，初始15
- `health`(int)：范围50~500，初始100（同时作为HP上限）
- `luck`(int)：范围1~30，初始10
- `current_hp`(int)：初始值等于`health`
- `current_mp`(int)：蓝条，仅使用技能时消耗，初始100，上限500
- `level`(int)：等级，初始1
- `exp`(int)：经验值，初始0，升级所需经验为阶梯式（如每级所需经验递增）

**实现方法：**

- `init_player_attributes()`：构造函数，赋予上述初始值。
- `get_attr(attr_name)`：返回指定属性值。**attr_name使用 shared_attributes.h中的常量字符串**。
- `modify_attr(attr_name, value, mode="add")`：修改属性。`mode`为"add"时增减（受范围限制），为"set"时直接设置（同样限制在上下限内）。**attr_name使用常量**。
- `gain_exp(int amount)`：增加经验值，若达到当前等级阶梯所需经验则升级（`level++`，`exp`减去所需经验），并提升属性（如每级提升`health`上限等）。

#### 功能2：HP与MP实时计算与扣减逻辑

**参数（共3项）：**

- `current_hp`(int)：实时变动
- `current_mp`(int)：实时变动
- `is_dead`(bool)：标记是否死亡

**实现方法：**

- `take_damage(int amount)`：扣除HP，`current_hp = max(0, current_hp - amount)`；若结果为0，调用`check_death()`。返回`bool`：`true`表示扣减后存活，`false`表示死亡。**（与 game_api.h一致，模块2 trigger_talisman返回的伤害值将传入此函数）**
- `heal(int amount)`：恢复HP，`current_hp = min(health, current_hp + amount)`。
- `consume_mp(int amount)`：消耗MP，`current_mp = max(0, current_mp - amount)`；返回`bool`表示是否足够。
- `restore_mp(int amount)`：恢复MP，`current_mp = min(500, current_mp + amount)`。
- `check_death()`：当`current_hp <= 0`时，设置`is_dead = true`，触发游戏结束或复活剧情。

#### 功能3：回合制战斗流程控制

**参数（共6项）：**

- `battle_state`(enum)：{NOT_IN_BATTLE, PLAYER_TURN, ENEMY_TURN, BATTLE_END}
- `turn_count`(int)：回合数，从1开始
- `action_queue`(list)：存储本回合待执行的动作（如攻击、使用道具）
- `current_enemies`(list)：当前战斗中的敌人对象列表
- `player_action_done`(bool)：标记玩家本回合是否已行动
- `action_points`(int)：本回合剩余行动点数，初始为1（若手持兔符咒则为2），每执行一个动作消耗1点，耗尽则结束回合。**（兔符咒额外行动通过 grant_extra_action()增加，仅限玩家回合生效）**

**实现方法：**

- `start_battle(enemy_list)`：初始化战斗状态，设置`battle_state = PLAYER_TURN`，清空队列，`turn_count = 1`，并根据手持符咒设置`action_points`（持有兔符咒时设为2）。
- `player_action(action_type, target)`：处理玩家输入（攻击、技能、道具等），加入`action_queue`，消耗1点`action_points`；若`action_points`耗尽，标记`player_action_done = true`并调用`end_turn()`。
- `enemy_action()`：遍历`current_enemies`，根据敌人AI决定行动并加入队列。
- `process_actions()`：依次执行`action_queue`中的动作，计算伤害/效果。**内部调用模块2的 int trigger_talisman(int talisman_id, Entity\* caster)获取伤害值（int），然后调用 take_damage应用伤害。**
- `end_turn()`：回合结束，调用`tick_cooldowns()`（来自功能4），减少Buff/Debuff持续回合，切换`battle_state`。
- `check_battle_end()`：检查胜负条件（所有敌人HP<=0或玩家`is_dead`）。

#### 功能4：冷却计数规则

**参数（共2项）：**

- `cooldown_dict`(map<int, int>)：键为技能/符咒ID（int），值为剩余冷却回合数
- `global_turn_counter`(int)：仅玩家行动回合递增（敌人回合不计入）

**实现方法：**

- `start_cooldown(int key, int turns)`：设置`cooldown_dict[key] = turns`。
- `tick_cooldowns()`：每玩家回合结束时，遍历`cooldown_dict`，将所有value > 0的减1。
- `is_on_cooldown(int key)`：返回`cooldown_dict[key] > 0`。
- `get_remaining_cooldown(int key)`：返回具体剩余回合数。

#### 功能5：敌人属性分档配置

**参数（共3档模板，每档4个属性）：**

- 杂兵模板：`hp_range`(30~50), `strength_range`(8~12), `reward_money`(5~50), `exp_drop`(10~20)
- 精英模板：`hp_range`(150~200), `strength_range`(25~30), `reward_money`(视剧情), `exp_drop`(50~100)
- Boss模板：`hp_range`(400~600), `strength_range`(40~55), `reward_money`(大额), `exp_drop`(200~500)

**实现方法：**

- `load_enemy_templates()`：从配置文件或常量中读取上述模板数据。
- `generate_enemy(type, difficulty=1)`：根据类型（杂兵/精英/Boss）和难度系数，在对应范围内随机生成敌人实例，返回敌人对象（包含当前HP、力量、exp_drop等）。

#### 功能6：与其他模块的交互接口

**参数（无独立存储，主要为函数接口）：**

- 预留回调函数或事件钩子

**实现方法：**

- `on_status_effect_applied(int effect_id, int duration)`：供状态效果模块调用，将效果加入战斗引擎的状态列表。
- ~~`on_talisman_triggered(int talisman_id, Player* caster)`~~：**已移除**（跨模块修补标记其为预留，不在头文件中声明，避免误用）。
- `grant_extra_action()`：供符咒模块（兔符咒）调用，增加1点`action_points`（若本回合未结束）。**限制：仅在玩家回合（battle_state == PLAYER_TURN）且回合未结束时生效。**
- `request_battle_log(const std::string& message)`：输出战斗日志，供剧情/NPC模块显示。