# 模块1：角色属性与战斗核心引擎（新架构适配版）

## 继承关系

cpp

```
class BattleEngine : public IBattleEngine { ... };
```

实现 `game_api.h`中定义的 `IBattleEngine`全部纯虚方法，并扩展内部实现。

------

## 共享头文件与前置声明

cpp

```
#include "game_api.h"           // 包含 IBattleEngine、TalismanID、StatusID、BattleResult 等
#include "character_fwd.h"      // 模块1自身提供，内含 class Player; class Enemy; 前置声明
```

> **变更说明**：原设计引用 `shared_attributes.h`和 `character_fwd.h`。新架构下 `game_api.h`已包含所有公共枚举/结构体/前置声明。`shared_attributes.h`中定义的属性名常量（如 `ATTR_STRENGTH`、`ATTR_HP`）仍保留在模块1内部使用，不放入 `game_api.h`（属于模块1内部实现细节）。

------

## 功能1：基础属性定义与初始化

**参数（共7项，money 由模块4管理）：**

- `strength`(int)：范围1~99，初始15
- `health`(int)：范围50~500，初始100（同时作为HP上限）
- `luck`(int)：范围1~30，初始10
- `current_hp`(int)：初始值等于 `health`
- `current_mp`(int)：蓝条，仅使用技能时消耗，初始100，上限500
- `level`(int)：等级，初始1
- `exp`(int)：经验值，初始0，升级所需经验为阶梯式

**实现方法：**

- `BattleEngine()`构造函数：调用 `init_player_attributes()`赋予初始值
- `init_player_attributes()`：赋予上述初始值
- `get_attr(attr_name)`：返回指定属性值。`attr_name`使用模块1内部 `shared_attributes.h`中的常量字符串
- `modify_attr(attr_name, value, mode="add")`：修改属性。`mode`为 `"add"`时增减（受范围限制），为 `"set"`时直接设置（同样限制在上下限内）
- `gain_exp(int amount)`：增加经验值，若达到当前等级阶梯所需经验则升级（`level++`，`exp`减去所需经验），并提升属性

> **注意**：`get_attr`/`modify_attr`是模块1内部方法，不放入 `IBattleEngine`接口（其他模块不需要直接操作玩家属性，如需查询可通过后续扩展接口）。

------

## 功能2：HP与MP实时计算与扣减逻辑

**参数（共3项）：**

- `current_hp`(int)：实时变动
- `current_mp`(int)：实时变动
- `is_dead`(bool)：标记是否死亡

**实现方法：**

- `take_damage(int amount)`：扣除HP，`current_hp = max(0, current_hp - amount)`；若结果为0，调用 `check_death()`。返回 `bool`：`true`表示存活，`false`表示死亡
- `heal(int amount)`：恢复HP，`current_hp = min(health, current_hp + amount)`
- `consume_mp(int amount)`：消耗MP，`current_mp = max(0, current_mp - amount)`；返回 `bool`表示是否足够
- `restore_mp(int amount)`：恢复MP，`current_mp = min(500, current_mp + amount)`
- `check_death()`：当 `current_hp <= 0`时，设置 `is_dead = true`，触发游戏结束或复活剧情

------

## 功能3：回合制战斗流程控制（实现 IBattleEngine 接口）

**参数（共6项）：**

- `battle_state`(enum)：{NOT_IN_BATTLE, PLAYER_TURN, ENEMY_TURN, BATTLE_END}
- `turn_count`(int)：回合数，从1开始
- `action_queue`(list)：存储本回合待执行的动作
- `current_enemies`(std::vector<Enemy*>)：当前战斗中的敌人对象列表
- `player_action_done`(bool)：标记玩家本回合是否已行动
- `action_points`(int)：本回合剩余行动点数，初始为1（持有兔符咒时为2），每执行一个动作消耗1点

**实现 IBattleEngine 接口的方法：**

- `void start_battle(const std::vector<Enemy*>& enemies) override`：初始化战斗状态，设置 `battle_state = PLAYER_TURN`，清空队列，`turn_count = 1`，并根据是否持有兔符咒（通过 `ITalismanManager::has_talisman(TalismanID::RABBIT)`查询）设置 `action_points`
- `void process_actions() override`：依次执行 `action_queue`中的动作，计算伤害/效果
- `BattleResult get_battle_result() const override`：返回当前战斗结果（战斗未结束时返回 `BattleResult::FLEE`或进行中状态，由实现类定义）
- `bool is_battle_active() const override`：返回 `battle_state != NOT_IN_BATTLE && battle_state != BATTLE_END`

**模块1内部方法（不放入接口）：**

- `player_action(action_type, target)`：处理玩家输入，加入 `action_queue`，消耗 `action_points`；耗尽则调用 `end_turn()`
- `enemy_action()`：遍历 `current_enemies`，根据AI决定行动并加入队列
- `end_turn()`：回合结束，调用 `tick_cooldowns()`，切换 `battle_state`
- `check_battle_end()`：检查胜负条件，若结束则设置 `battle_result`并通知模块5（通过 `ILevelManager*`回调）

**跨模块调用变更：**

- 原设计：`process_actions()`内部调用模块2的 `int trigger_talisman(int talisman_id, Entity* caster)`获取伤害值
- 新架构：`process_actions()`内部调用 `ITalismanManager::trigger_talisman(TalismanID id, Entity* caster)`返回 `TalismanID`（触发结果），伤害计算由战斗引擎根据符咒效果自行处理

------

## 功能4：冷却计数规则

**参数（共2项）：**

- `cooldown_dict`(std::map<TalismanID, int>)：键为符咒ID（TalismanID），值为剩余冷却回合数
- `global_turn_counter`(int)：仅玩家行动回合递增

**实现方法：**

- `start_cooldown(TalismanID key, int turns)`：设置 `cooldown_dict[key] = turns`
- `tick_cooldowns()`：每玩家回合结束时，遍历 `cooldown_dict`，将所有 value > 0 的减1
- `is_on_cooldown(TalismanID key)`：返回 `cooldown_dict[key] > 0`
- `get_remaining_cooldown(TalismanID key)`：返回具体剩余回合数

------

## 功能5：敌人属性分档配置

**参数（共3档模板，每档4个属性）：**

- 杂兵模板：`hp_range`(30~50), `strength_range`(8~12), `reward_money`(5~50), `exp_drop`(10~20)
- 精英模板：`hp_range`(150~200), `strength_range`(25~30), `reward_money`(视剧情), `exp_drop`(50~100)
- Boss模板：`hp_range`(400~600), `strength_range`(40~55), `reward_money`(大额), `exp_drop`(200~500)

**实现方法：**

- `load_enemy_templates()`：从配置文件或常量中读取上述模板数据
- `generate_enemy(type, difficulty=1)`：根据类型和难度系数随机生成敌人实例，返回 `Enemy*`

------

## 功能6：与其他模块的交互接口

**跨模块指针（通过抽象基类，由外部注入）：**

cpp

```
ITalismanManager* talisman_mgr;   // 模块2
IStatusEffectManager* status_mgr; // 模块3
ILevelManager* level_mgr;         // 模块5（回调用）
```

**注入方法（实现类内部，不放入 IBattleEngine 接口）：**

- `set_talisman_manager(ITalismanManager* mgr)`
- `set_status_effect_manager(IStatusEffectManager* mgr)`
- `set_level_manager(ILevelManager* mgr)`

**交互方法：**

- `on_status_effect_applied(Entity* target, StatusID id, int duration)`：供模块3调用，将效果加入战斗引擎状态列表（**注意**：原设计参数为 `int effect_id`，现改为 `StatusID id`）
- `grant_extra_action()`：供模块2（兔符咒）调用，增加1点 `action_points`（限制：仅在 `battle_state == PLAYER_TURN`且回合未结束时生效）
- `request_battle_log(const std::string& message)`：输出战斗日志，供模块5（剧情/NPC）显示

> **移除内容**：原设计中的 `on_talisman_triggered`已移除，避免跨模块误用。