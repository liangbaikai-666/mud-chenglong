现在进入第4个问题：**检查所有模块间的接口一致性**（逐条比对调用方和被调用方的签名是否匹配，输出检查报告）。

基于已确认的 `game_api.h`以及模块1~5的C++类设计，以下是逐条比对结果：

### 一、模块1（BattleEngine）↔ 模块2（TalismanManager）

| 函数签名（被调用方声明）                       | 调用方使用方式                           | 匹配状态 | 备注                                                   |
| ---------------------------------------------- | ---------------------------------------- | -------- | ------------------------------------------------------ |
| `int trigger_talisman(int id, Entity* caster)` | 模块1接收返回值int，传入int和Entity*     | ✅ 一致   | 修补版已确认返回int伤害                                |
| `bool can_trigger(int id) const`               | 模块1传入int，判断bool返回值             | ✅ 一致   |                                                        |
| `void on_turn_end()`                           | 模块1在回合结束时调用                    | ✅ 一致   |                                                        |
| `void on_battle_start()`                       | 模块1在战斗开始时调用                    | ✅ 一致   |                                                        |
| 回调：兔符咒通知战斗引擎增加行动点             | 模块2通过回调/事件通知，战斗引擎内部处理 | ✅ 一致   | 回调机制在game_api.h中通过注释说明，不直接暴露函数签名 |

### 二、模块1（BattleEngine）↔ 模块3（StatusEffectManager）

| 函数签名（被调用方声明）                                     | 调用方使用方式                     | 匹配状态 | 备注                    |
| ------------------------------------------------------------ | ---------------------------------- | -------- | ----------------------- |
| `void on_turn_end()`                                         | 模块1在回合结束时调用              | ✅ 一致   |                         |
| `void on_battle_start()`                                     | 模块1在战斗开始时调用              | ✅ 一致   |                         |
| `bool has_status(Entity* target, const std::string& id) const` | 模块1传入Entity*和string，判断bool | ✅ 一致   | 用于检查RESIST_UP等状态 |

### 三、模块2（TalismanManager）↔ 模块3（StatusEffectManager）

| 函数签名（被调用方声明）                                     | 调用方使用方式                | 匹配状态 | 备注               |
| ------------------------------------------------------------ | ----------------------------- | -------- | ------------------ |
| `void on_talisman_effect_request(int talisman_id, Entity* caster)` | 模块2传入int和Entity*         | ✅ 一致   | 常驻符咒触发时调用 |
| `void add_status(Entity* target, const std::string& id, int duration)` | 模块2传入Entity*、string、int | ✅ 一致   | 直接添加Buff       |
| `void remove_status(Entity* target, const std::string& id)`  | 模块2传入Entity*、string      | ✅ 一致   | 移除状态           |

### 四、模块3（StatusEffectManager）↔ 模块2（TalismanManager）

| 函数签名（被调用方声明）                                     | 调用方使用方式    | 匹配状态 | 备注           |
| ------------------------------------------------------------ | ----------------- | -------- | -------------- |
| `void on_status_effect_applied(int effect_id, int duration)` | 模块3传入int、int | ✅ 一致   | 状态应用后回调 |
| `void request_effect_removal(int effect_id)`                 | 模块3传入int      | ✅ 一致   | 请求移除效果   |

### 五、模块4（InventoryManager）↔ 模块1（Player）

| 函数签名（被调用方声明）              | 调用方使用方式       | 匹配状态 | 备注        |
| ------------------------------------- | -------------------- | -------- | ----------- |
| `void Player::heal(int amount)`       | 模块4 use_item中调用 | ✅ 一致   | 恢复HP      |
| `void Player::consume_mp(int amount)` | 模块4 use_item中调用 | ✅ 一致   | 恢复/消耗MP |

### 六、模块4（InventoryManager）↔ 模块3（StatusEffectManager）

| 函数签名（被调用方声明）                                     | 调用方使用方式                                | 匹配状态 | 备注         |
| ------------------------------------------------------------ | --------------------------------------------- | -------- | ------------ |
| `void add_status(Entity* target, const std::string& id, int duration)` | 模块4传入Player*（继承自Entity）、string、int | ✅ 一致   | 道具施加状态 |

### 七、模块5（LevelManager）↔ 模块1（BattleEngine）

| 函数签名（被调用方声明）                                   | 调用方使用方式    | 匹配状态 | 备注     |
| ---------------------------------------------------------- | ----------------- | -------- | -------- |
| `void start_battle(const std::vector<Enemy*>& enemy_list)` | 模块5传入敌人列表 | ✅ 一致   | 启动战斗 |

### 八、模块5（LevelManager）↔ 模块2（TalismanManager）

| 函数签名（被调用方声明）       | 调用方使用方式         | 匹配状态 | 备注         |
| ------------------------------ | ---------------------- | -------- | ------------ |
| `void add_to_backpack(int id)` | 模块5传入int（符咒ID） | ✅ 一致   | 自动补全符咒 |

### 九、模块5（LevelManager）↔ 模块3（StatusEffectManager）

| 函数签名（被调用方声明）                                     | 调用方使用方式                | 匹配状态 | 备注         |
| ------------------------------------------------------------ | ----------------------------- | -------- | ------------ |
| `void add_status(Entity* target, const std::string& id, int duration)` | 模块5传入Player*、string、int | ✅ 一致   | 剧情强制状态 |

### 十、模块5（LevelManager）↔ 模块4（InventoryManager）

| 函数签名（被调用方声明）                               | 调用方使用方式                 | 匹配状态 | 备注     |
| ------------------------------------------------------ | ------------------------------ | -------- | -------- |
| `void add_money(int amount)`                           | 模块5传入int                   | ✅ 一致   | 发放金钱 |
| `bool add_item(const std::string& item_id, int count)` | 模块5传入string、int，接收bool | ✅ 一致   | 发放道具 |

------

### 检查结论

**所有跨模块接口签名完全匹配，无不一致项。** 各模块仅依赖 `game_api.h`中的声明，可以并行开发。