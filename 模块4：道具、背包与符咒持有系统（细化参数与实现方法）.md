# 模块4：道具、背包与金钱系统（新架构适配版）

## 继承关系

cpp

```
class InventoryManager : public IInventoryManager { ... };
```

实现 `game_api.h`中定义的 `IInventoryManager`全部纯虚方法，并扩展内部实现。

> **职责调整说明**：新架构中，符咒的持有、切换、效果由模块2（`ITalismanManager`）全权管理。模块4仅负责**道具（药水等）和金钱**的存储与使用，不再存储符咒数据。原设计中与符咒相关的功能（功能2）已移至模块2实现，模块4通过调用 `ITalismanManager`接口与符咒系统协作。

------

## 共享头文件与前置声明

cpp



```
#include "game_api.h"           // 包含 IInventoryManager、ItemID、TalismanID 等
#include "character_fwd.h"      // 模块1提供，内含 class Entity; class Player; class Enemy; 前置声明
```

> **变更说明**：原设计引用 `shared_attributes.h`（道具ID字符串常量）。新架构下 `ItemID`已定义为 enum class（POTION_HP=1, POTION_MP=2，其余预留扩展），不再需要字符串ID。`game_api.h`已包含 `Entity`前置声明。

------

## 功能1：背包存储与容量管理（实现 IInventoryManager 接口）

**参数（共3项）：**

- `item_backpack`(std::map<ItemID, int>)：道具ID与数量的映射，上限10格（不同道具各占1格，同种道具堆叠计数）。
- `max_item_slots`(int)：道具栏最大容量，固定10。
- `item_effects`(std::map<ItemID, ItemEffectConfig>)：道具效果配置表（从文件加载，内部使用）。

**实现 IInventoryManager 接口的方法：**

- `bool add_item(ItemID item_id, int count) override`：若道具已存在则堆叠count，否则占用新格子。返回bool表示是否成功（容量不足则false）。
- `bool remove_item(ItemID item_id, int count) override`：减少指定道具数量，归零后移除格子。返回bool（数量不足返回false）。
- `bool has_item(ItemID item_id) const override`：返回 `item_backpack.count(item_id) > 0`。
- `int get_item_count(ItemID item_id) const override`：返回该道具的当前数量，不存在返回0。

**模块4内部方法（不放入接口）：**

- `void sort_backpack()`：按道具ID排序背包（UI显示用）。

------

## 功能2：道具使用与效果执行（实现 IInventoryManager 接口）

**参数（共2项）：**

- `battle_context`(IBattleEngine*)：当前战斗引擎指针（通过抽象基类），用于判断是否在战斗中。
- `use_message`(std::string)：使用后的反馈文本（内部使用）。

**实现 IInventoryManager 接口的方法：**

- `void use_item(ItemID item_id, Entity* target) override`：检查 `has_item(item_id)`。根据 `item_id`查找效果配置。执行对应逻辑：回血类（POTION_HP）：调用 `target->heal(amount)`（需将 `Entity*`转为 `Player*`，内部实现中处理）。回蓝类（POTION_MP）：调用 `target->restore_mp(amount)`。其他效果：按配置执行。从背包移除该道具（消耗品，调用 `remove_item(item_id, 1)`）。

**跨模块调用：**

- 使用道具后如需清除Debuff，调用 `IStatusEffectManager::clear_all_statuses(target)`或预留的清除接口（通过 `status_mgr`指针）。

------

## 功能3：道具掉落与拾取

**参数（共1项）：**

- `drop_table`(std::map<int, std::vector<std::pair<ItemID, float>>>)：敌人类型（int，与模块1一致）对应的掉落物ID与概率表。

**实现方法：**

- `std::vector<ItemID> generate_drops(int enemy_type)`：根据掉落表随机生成掉落列表，逐个调用 `add_item`加入背包。
- `bool pickup_item(ItemID item_id, int count = 1)`：调用 `add_item`，成功则加入背包，失败返回false（提示背包已满）。
- `void pickup_money(int amount)`：直接调用 `add_money(amount)`。

------

## 功能4：金钱管理（实现 IInventoryManager 接口）

**参数（共1项）：**

- `money`(int)：当前持有金钱，无上限。

**实现 IInventoryManager 接口的方法：**

- `void add_money(int amount) override`：增加金钱。
- `bool consume_money(int amount) override`：扣除金钱，返回bool（余额不足返回false）。
- `int get_money() const override`：返回当前金钱值。

------

## 功能5：老爹商店交互接口

**参数（共2项）：**

- `shop_inventory`(std::map<int, std::vector<ItemID>>)：键为解锁进度（对应模块5的进度），值为该进度下可购买的商品ID列表。
- `current_progress`(int)：玩家当前闯关进度标记（由模块5提供）。

**实现方法：**

- `std::vector<ItemID> get_available_items(int progress) const`：根据进度返回可购买的商品ID列表。
- `bool buy_item(ItemID item_id, int progress)`：检查金钱是否足够、背包是否有空位，扣除金钱并调用 `add_item`。
- `void request_full_heal(Player* player)`：免费（或消耗特定道具）将玩家HP回满、清除所有Debuff（调用 `IStatusEffectManager`的清除接口），仅限安全区调用。

------

## 功能6：与战斗引擎、符咒模块、状态模块的交互

**跨模块指针（通过抽象基类，由外部注入）：**

cpp



```
IBattleEngine* battle_engine;           // 模块1（查询战斗状态）
ITalismanManager* talisman_mgr;         // 模块2（符咒相关查询）
IStatusEffectManager* status_mgr;        // 模块3（清除Debuff等）
ILevelManager* level_mgr;               // 模块5（查询进度）
```

**注入方法（实现类内部，不放入 IInventoryManager 接口）：**

- `set_battle_engine(IBattleEngine* engine)`
- `set_talisman_manager(ITalismanManager* mgr)`
- `set_status_effect_manager(IStatusEffectManager* mgr)`
- `set_level_manager(ILevelManager* mgr)`

**交互方法：**

- `on_item_used_in_battle(ItemID item_id)`：通知战斗引擎（模块1）消耗了1个行动回合（通过回调或事件）。
- `on_talisman_acquired(TalismanID id)`：新符咒获取时，调用 `talisman_mgr->add_to_backpack(id)`加入符咒系统。
- `on_battle_start()`：通知符咒模块重置临时状态（调用 `talisman_mgr->on_battle_start()`）。