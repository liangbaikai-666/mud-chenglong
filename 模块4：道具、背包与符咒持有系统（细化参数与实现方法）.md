#### 模块4：道具、背包与符咒持有系统（修补版）

**共享头文件与前置声明**

- `#include "shared_attributes.h"`（道具ID、属性名等常量）
- `#include "character_fwd.h"`（内含 `class Player;`前置声明，供 `use_item`等函数使用）

#### 功能1：背包存储与容量管理

**参数（共4项）：**

- `item_backpack`(vector<pair<string, int>>)：道具ID与数量的列表，上限10格（不同道具各占1格）。
- `talisman_backpack`(vector<int>)：符咒ID列表，上限12枚（类型 `int`，与模块2一致）。
- `max_item_slots`(int)：道具栏最大容量，固定10。
- `max_talisman_slots`(int)：符咒栏最大容量，固定12。

**实现方法：**

- `bool add_item(const std::string& item_id, int count)`：若道具已存在则堆叠count，否则占用新格子。返回bool表示是否成功（容量不足则false）。
- `void remove_item(const std::string& item_id, int count)`：减少指定道具数量，归零后移除格子。
- `bool has_item(const std::string& item_id) const`：返回bool，检查道具是否存在于背包。
- `int get_item_count(const std::string& item_id) const`：返回该道具的当前数量。
- `void sort_backpack()`：按道具类型/ID排序背包。

#### 功能2：符咒持有与切换（与模块2协作）

**参数（共3项）：**

- `handheld`(array<int, 2>)：当前手持符咒ID，空槽为 `-1`（类型 `int`，与模块2一致）。
- `is_switching`(bool)：是否正在切换中。
- `switch_target`(int)：待切换的符咒ID（类型 `int`）。

**实现方法：**

- `void equip_talisman(int slot, int id)`：将指定槽位设为id，并**调用模块2的 TalismanManager::equip_to_hand(slot, id)** 应用常驻效果（接口签名见功能7）。
- `void unequip_talisman(int slot)`：清空槽位，调用模块2的 `TalismanManager::unequip_from_hand(slot)`移除效果。
- `void initiate_switch(int new_id)`：设置切换标志和目标，等待战斗引擎在回合结束时调用 `finalize_switch()`。
- `void finalize_switch()`：完成切换，更新 `handheld`，调用模块2更新手持状态。

#### 功能3：道具掉落与拾取

**参数（共1项）：**

- `drop_table`(map<int, vector<pair<string, float>>>)：敌人类型（int，与模块1一致）对应的掉落物ID与概率表。

**实现方法：**

- `vector<string> generate_drops(int enemy_type)`：根据掉落表随机生成掉落列表，逐个调用 `pickup_item`加入背包。
- `bool pickup_item(const std::string& item_id)`：调用 `add_item`，成功则加入背包，失败返回false（提示背包已满）。
- `void pickup_money(int amount)`：直接调用 `add_money(amount)`。

#### 功能4：道具使用与效果执行

**参数（共3项）：**

- `battle_context`(BattleEngine*)：当前战斗指针，为空表示非战斗状态。
- `item_effects`(map<string, ItemEffectConfig>)：道具效果配置表（从文件加载）。
- `use_message`(string)：使用后的反馈文本。

**实现方法：**

- `bool use_item(const std::string& item_id, Player* user, Player* target)`：**签名与 game_api.h一致**。检查是否在背包中。根据 `item_id`查找效果配置。执行对应逻辑：回血类：调用 `user->heal(amount)`或 `target->heal(amount)`。清Debuff类：调用**模块3的 StatusEffectManager::clear_statuses_by_type(StatusType::DEBUFF)**。其他效果：按配置执行。从背包移除该道具（消耗品）。返回bool表示是否使用成功。

#### 功能5：金钱管理

**参数（共1项）：**

- `money`(int)：当前持有金钱，无上限。

**实现方法：**

- `void add_money(int amount)`：增加金钱。
- `bool spend_money(int amount)`：扣除金钱，返回bool（余额不足返回false）。
- `int get_money() const`：返回当前金钱值。

#### 功能6：老爹商店交互接口

**参数（共2项）：**

- `shop_inventory`(map<int, vector<string>>)：键为解锁进度（L1/L2/L3，对应模块5的进度），值为该进度下可购买的商品ID列表。
- `current_progress`(int)：玩家当前闯关进度标记。

**实现方法：**

- `vector<string> get_available_items()`：根据 `current_progress`返回可购买的商品ID列表。
- `bool buy_item(const std::string& item_id)`：检查金钱是否足够、背包是否有空位，扣除金钱并调用 `add_item`。
- `void request_full_heal()`：免费（或消耗特定道具）将玩家HP回满、清除所有Debuff（调用模块3的清除接口），仅限安全区调用。

#### 功能7：与战斗引擎、符咒模块、状态模块的交互（外部接口依赖）

**参数（无独立存储，主要为事件回调）：**

- 模块间通信接口指针。

**实现方法：**

- `void on_battle_start()`：通知模块2重置临时状态（调用 `TalismanManager::on_battle_start()`）。
- `void on_talisman_acquired(int id)`：新符咒加入背包，调用模块2的 `TalismanManager::add_to_backpack(id)`（注意：模块5的 `auto_grant_missing_talismans`也直接调用此函数，不经过本模块）。
- `void on_item_used_in_battle(const std::string& item_id)`：通知战斗引擎（模块1）消耗了1个行动回合（通过回调或事件）。
- **外部接口依赖（准确签名）**：模块2 `TalismanManager`：`void equip_to_hand(int slot, int id);``void unequip_from_hand(int slot);``void add_to_backpack(int id);`模块3 `StatusEffectManager`：`void clear_statuses_by_type(StatusType type);`模块1 `Player`：`void heal(int amount);`