# 模块1 C++ 类设计（新架构版）

基于 `game_api.h`抽象基类架构，以下是模块1（角色属性与战斗核心引擎）的C++类设计。**返回值与实现思路写在注释中，不提供具体实现，由组员自行编写。**

cpp

```cpp
#ifndef BATTLE_ENGINE_IMPL_H
#define BATTLE_ENGINE_IMPL_H

#include "game_api.h"       // 包含 IBattleEngine、TalismanID、StatusID、BattleResult 等
#include <string>
#include <vector>
#include <map>
#include <list>

// ==================== 战斗状态枚举（模块1内部使用） ====================
enum class BattleState {
    NOT_IN_BATTLE,
    PLAYER_TURN,
    ENEMY_TURN,
    BATTLE_END
};

// ==================== 动作类型枚举（模块1内部使用） ====================
enum class ActionType {
    ATTACK,
    USE_SKILL,
    USE_ITEM,
    USE_TALISMAN,
    FLEE
};

// ==================== 战斗动作结构体（模块1内部使用） ====================
struct BattleAction {
    ActionType type;       // 动作类型
    Entity* actor;         // 行动者
    Entity* target;        // 目标
    int value;             // 附加数值（技能ID/道具ID/符咒ID等，视type而定）
};

// ==================== Player 类 ====================
class Player {
private:
    // ---- 属性成员（来自功能1） ----
    int current_hp;        // 当前HP，初始100
    int max_hp;            // HP上限，初始100，范围50~500
    int current_mp;        // 当前MP，初始100，上限500
    int level;             // 等级，初始1
    int exp;               // 经验值，初始0
    int base_strength;     // 基础力量，初始15，范围1~99
    int base_luck;         // 基础运气，初始10，范围1~30
    // 注：money 已移至模块4（IInventoryManager），此处不再保留

public:
    // ---- 构造函数 ----
    Player();

    // ---- 功能1：属性获取与修改 ----
    /**
     * 获取指定属性值
     * @param attr_name 属性名字符串（模块1内部常量，如 "strength"/"health" 等）
     * @return int 对应属性的当前值
     * 实现思路：if-else 判断 attr_name，返回对应成员变量的值
     */
    int get_attr(const std::string& attr_name) const;

    /**
     * 修改属性（增加或设置）
     * @param attr_name 属性名字符串
     * @param value 变化量或目标值
     * @param mode "add"=增减（受范围限制），"set"=直接设置（受上下限限制）
     * @return void
     * 实现思路：根据 attr_name 找到对应成员，按 mode 修改，并用 std::clamp 限制到合法范围
     *          例如 strength 范围1~99，hp 范围0~max_hp
     */
    void modify_attr(const std::string& attr_name, int value, const std::string& mode = "add");

    // ---- 功能2：HP/MP实时计算与扣减 ----
    /**
     * 承受伤害
     * @param amount 伤害数值
     * @return bool true=存活，false=死亡（current_hp 降为0或以下）
     * 实现思路：current_hp = std::max(0, current_hp - amount);
     *          若 current_hp == 0，调用 check_death()
     */
    bool take_damage(int amount);

    /**
     * 恢复HP
     * @param amount 恢复量
     * @return void
     * 实现思路：current_hp = std::min(max_hp, current_hp + amount);
     */
    void heal(int amount);

    /**
     * 消耗MP
     * @param amount 消耗量
     * @return bool true=足够并扣除，false=不足
     * 实现思路：if current_mp >= amount，扣除并返回 true; else 返回 false
     */
    bool consume_mp(int amount);

    /**
     * 恢复MP
     * @param amount 恢复量
     * @return void
     * 实现思路：current_mp = std::min(500, current_mp + amount);
     */
    void restore_mp(int amount);

    // ---- 功能1扩展：经验与升级 ----
    /**
     * 获得经验值
     * @param amount 获得的经验
     * @return void
     * 实现思路：exp += amount; 循环检查是否达到 level * 100 阶梯，
     *          若达到则 level++，exp -= 所需经验，提升 max_hp/base_strength/base_luck 等属性
     */
    void gain_exp(int amount);

    // ---- getter（供战斗引擎等外部读取） ----
    int get_current_hp() const;
    int get_max_hp() const;
    int get_level() const;
    int get_strength() const;
    bool is_alive() const;
};

// ==================== BattleEngine 类（继承 IBattleEngine） ====================
class BattleEngine : public IBattleEngine {
private:
    // ---- 功能3成员：战斗流程 ----
    BattleState battle_state;              // 战斗状态（内部枚举，非 GameState）
    int turn_count;                        // 回合数，从1开始
    std::list<BattleAction> action_queue;  // 本回合待执行的动作队列
    std::vector<Enemy*> current_enemies;   // 当前战斗中的敌人列表
    bool player_action_done;               // 玩家本回合是否已行动完毕
    int action_points;                     // 本回合剩余行动点（1或2，兔符咒时为2）

    // ---- 功能4成员：冷却系统 ----
    std::map<TalismanID, int> cooldown_dict;  // 符咒冷却，键为 TalismanID enum class
    int global_turn_counter;                  // 全局回合计数器

    // ---- 玩家指针 ----
    Player* player;

    // ---- 跨模块指针（通过抽象基类，由外部注入） ----
    ITalismanManager* talisman_mgr;
    IStatusEffectManager* status_mgr;
    ILevelManager* level_mgr;

public:
    // ---- 构造函数 ----
    /**
     * @param p 玩家指针（由外部创建并传入）
     * 实现思路：初始化所有成员为默认值，调用 player->gain_exp(0) 触发初始属性设置
     */
    BattleEngine(Player* p);

    // ==================== IBattleEngine 接口实现 ====================
    /**
     * 开始战斗
     * @param enemies 敌人列表
     * @return void
     * 实现思路：设置 battle_state = PLAYER_TURN，清空 action_queue，
     *          turn_count = 1，拷贝 enemies 到 current_enemies。
     *          根据 talisman_mgr->has_talisman(TalismanID::RABBIT) 设置 action_points（1或2）
     */
    void start_battle(const std::vector<Enemy*>& enemies) override;

    /**
     * 依次执行动作队列中的所有动作
     * @return void
     * 实现思路：循环从 action_queue 前端取出动作并执行。
     *          【跨模块调用】若动作涉及符咒，调用 talisman_mgr->trigger_talisman(TalismanID, Entity*)
     *          获取触发结果，再根据符咒效果计算伤害/状态并施加到目标
     */
    void process_actions() override;

    /**
     * 获取当前战斗结果
     * @return BattleResult 枚举值（WIN/LOSE/FLEE）
     * 实现思路：返回内部缓存的 battle_result_ 成员（战斗未结束时返回 FLEE 或进行中标记）
     */
    BattleResult get_battle_result() const override;

    /**
     * 查询战斗是否仍在进行中
     * @return bool true=进行中，false=已结束或未开始
     * 实现思路：返回 battle_state != NOT_IN_BATTLE && battle_state != BATTLE_END
     */
    bool is_battle_active() const override;

    // ==================== 跨模块指针注入（内部使用，非接口） ====================
    /**
     * 设置符咒管理器指针
     * @param mgr ITalismanManager 实现类指针
     * @return void
     * 实现思路：直接赋值 talisman_mgr = mgr;
     */
    void set_talisman_manager(ITalismanManager* mgr);

    /**
     * 设置状态效果管理器指针
     * @param mgr IStatusEffectManager 实现类指针
     * @return void
     */
    void set_status_effect_manager(IStatusEffectManager* mgr);

    /**
     * 设置关卡管理器指针
     * @param mgr ILevelManager 实现类指针
     * @return void
     */
    void set_level_manager(ILevelManager* mgr);

    // ==================== 模块1内部方法（不放入接口） ====================
    // ---- 功能3：战斗流程控制 ----
    /**
     * 处理玩家动作
     * @param type 动作类型（攻击/技能/道具/符咒）
     * @param target 目标实体指针
     * @param value 附加数值（如符咒ID对应的int值）
     * @return void
     * 实现思路：构造 BattleAction 加入 action_queue，消耗1点 action_points；
     *          若 action_points 耗尽，标记 player_action_done = true 并调用 end_turn()
     */
    void player_action(ActionType type, Entity* target, int value = 0);

    /**
     * 敌人回合行动
     * @return void
     * 实现思路：遍历 current_enemies，对存活敌人根据AI决定动作加入 action_queue
     */
    void enemy_action();

    /**
     * 结束当前回合
     * @return void
     * 实现思路：调用 process_actions() 执行剩余动作，然后 tick_cooldowns() 减少冷却，
     *          切换 battle_state（PLAYER_TURN → ENEMY_TURN → PLAYER_TURN），
     *          重置 action_points，递增 turn_count
     */
    void end_turn();

    /**
     * 检查战斗是否结束
     * @return void
     * 实现思路：若所有敌人HP<=0，设置 battle_state = BATTLE_END，缓存 WIN 结果，
     *          调用 level_mgr->on_battle_end(BattleResult::WIN, current_enemies)；
     *          若玩家死亡，缓存 LOSE 结果
     */
    void check_battle_end();

    // ---- 功能4：冷却计数 ----
    /**
     * 开始冷却
     * @param key 符咒ID（TalismanID enum class）
     * @param turns 冷却回合数
     * @return void
     * 实现思路：cooldown_dict[key] = turns;
     */
    void start_cooldown(TalismanID key, int turns);

    /**
     * 每玩家回合结束时减少所有冷却
     * @return void
     * 实现思路：遍历 cooldown_dict，所有 value > 0 的减1
     */
    void tick_cooldowns();

    // ---- 功能6：跨模块交互（供其他模块回调） ----
    /**
     * 兔符咒额外行动点
     * @return void
     * 实现思路：【限制】仅在 battle_state == PLAYER_TURN 且 player_action_done == false 时，
     *          增加 action_points += 1（由模块2符咒系统调用）
     */
    void grant_extra_action();

    /**
     * 状态效果施加通知
     * @param target 目标实体
     * @param id 状态ID（StatusID enum class）
     * @param duration 持续回合数
     * @return void
     * 实现思路：战斗引擎记录该状态效果，用于后续伤害计算修正（如虚弱减半力量）
     */
    void on_status_effect_applied(Entity* target, StatusID id, int duration);

    /**
     * 请求输出战斗日志
     * @param message 日志内容
     * @return void
     * 实现思路：将 message 传递给模块5（剧情/NPC系统）显示，或输出到控制台
     */
    void request_battle_log(const std::string& message);
};

#endif // BATTLE_ENGINE_IMPL_H
```

------

**说明：**

- 以上仅为 `.h`头文件框架，所有方法只声明、注释返回值意义与实现思路，**不含 .cpp 实现代码**。
- 组员根据注释中的"实现思路"自行编写 `.cpp`文件。
- 跨模块调用全部通过抽象基类指针（`ITalismanManager*`/ `IStatusEffectManager*`/ `ILevelManager*`），不直接依赖具体类。
- 符咒ID、状态ID全部使用 `game_api.h`中定义的 `enum class`类型。