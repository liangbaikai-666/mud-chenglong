# 模块5：关卡流程、场景交互与剧情调度（新架构适配版）

## 继承关系

cpp

```
class LevelManager : public ILevelManager { ... };
class DialogueManager : public IDialogueManager { ... };
class GameStateManager : public IGameStateManager { ... };
```

分别实现 `game_api.h`中定义的三个抽象基类全部纯虚方法，并扩展内部实现。

------

## 共享头文件与前置声明

cpp

```
#include "game_api.h"           // 包含三个接口、枚举、结构体、前置声明
#include "character_fwd.h"      // 模块1提供，内含 class Enemy; 前置声明
```

> **变更说明**：原设计引用 `shared_attributes.h`（关卡ID/道具ID字符串常量）。新架构下 `ItemID`/`TalismanID`/`StatusID`已定义为 enum class，`game_api.h`已包含所有公共定义。`shared_attributes.h`中定义的关卡ID常量仍保留在模块5内部使用（属于实现细节，不放入接口）。

------

## 功能1：关卡树结构与进度管理（对应 LevelManager，实现 ILevelManager 接口）

**参数（共5项）：**

- `current_layer`(int)：当前所在层，取值1~4（L1/L2/L3/终战）。
- `cleared_levels`(std::set<std::string>)：已通关的关卡ID集合。
- `unlocked_levels`(std::set<std::string>)：已解锁可进入的关卡ID集合。
- `all_talismans_collected`(bool)：是否集齐全套十二符咒。
- `level_configs`(std::map<std::string, LevelConfig>)：关卡配置表（LevelConfig 结构体在 `game_api.h`中定义，含 `id`、`layer`、`prerequisites`、`waves`、`difficulty_coef`、`rewards`）。

**战斗树结构（不变）：**

- L1：3个关卡
- L2：4个关卡
- L3：5个关卡
- L4：1个关卡（最终战）

**解锁规则（不变）：**

- L2解锁条件：L1全部3关打通。
- L3解锁条件：L2通过任意2关及以上。
- L4解锁条件：L3通过任意1关及以上。

**实现 ILevelManager 接口的方法：**

- `void init_progress() override`：初始化进度，解锁L1的3个关卡，其余锁定。
- `bool can_enter_level(const std::string& level_id) const override`：检查是否满足进入条件（逻辑同原设计）。
- `void on_level_cleared(const std::string& level_id) override`：将关卡加入 `cleared_levels`，更新 `current_layer`，按解锁规则更新 `unlocked_levels`。
- `void auto_grant_missing_talismans() override`：进入终战前调用，**通过 ITalismanManager\*指针调用 add_to_backpack(TalismanID id)** 将未收集的符咒加入背包（不经过模块4）。
- `void start_battle_for_level(const std::string& level_id) override`：加载关卡配置，生成敌人，调用 `IBattleEngine::start_battle(enemy_list)`。
- `void on_battle_end(BattleResult result, const std::vector<Enemy*>& defeated) override`：战斗结束回调，处理奖励发放和进度更新。
- `int get_current_layer() const override`：返回 `current_layer`。
- `bool is_all_talismans_collected() const override`：返回 `all_talismans_collected`。

------

## 功能2：关卡进入条件与分支选择（属于 LevelManager 内部逻辑）

**参数（共2项）：**

- `selected_branch`(std::string)：当前选择的分支ID。
- `enemy_wave_index`(int)：当前波次索引。

**实现方法（模块5内部，不放入接口）：**

- `LevelConfig load_level_config(const std::string& level_id)`：从配置表获取关卡配置。
- `void select_branch(const std::string& branch_id)`：设置 `selected_branch`，校验是否满足进入条件。
- `std::vector<Enemy*> generate_enemy_wave(int wave_index)`：根据关卡配置生成敌人列表（调用模块1的 `generate_enemy`，但由战斗引擎驱动）。
- `std::vector<std::string> get_available_branches()`：返回当前层所有可选分支ID列表。

------

## 功能3：战斗外NPC对话与剧情触发（对应 DialogueManager，实现 IDialogueManager 接口）

**参数（共3项）：**

- `npc_dialogue_table`(std::map<std::string, std::vector<DialogueNode>>)：NPC对话表，键为NPC名+进度阶段。
- `current_stage`(std::string)：当前剧情阶段标识（如 "L1_START"）。
- `active_npc`(std::string)：当前正在交互的NPC ID。

**实现 IDialogueManager 接口的方法：**

- `std::vector<DialogueNode> get_npc_dialogue(const std::string& npc_id) const override`：根据 `current_stage`返回对话列表。
- `void advance_stage(const std::string& new_stage) override`：更新剧情阶段。
- `std::string on_npc_interact(const std::string& npc_id) override`：返回当前阶段该NPC的对话内容。
- `std::string trigger_guide_hint(GuideType type) const override`：根据类型返回引导提示文本。

------

## 功能4：全局游戏状态管理（对应 GameStateManager，实现 IGameStateManager 接口）

**参数（共1项）：**

- `game_state`(GameState)：枚举值 `{PLAYING, STORY_DEATH, VICTORY, GAME_OVER}`。

**实现 IGameStateManager 接口的方法：**

- `GameState get_game_state() const override`：返回当前状态。
- `void check_story_death() override`：检查即死条件，若满足则设置 `game_state = STORY_DEATH`。
- `void on_victory() override`：设置 `game_state = VICTORY`。
- `void on_game_over() override`：设置 `game_state = GAME_OVER`。

------

## 功能5：战斗结算与奖励发放（属于 LevelManager 内部逻辑）

**参数（共3项）：**

- `battle_result`(BattleResult)：枚举值 `{WIN, LOSE, FLEE}`。
- `reward_pool`(RewardConfig)：包含金钱基数、道具掉落率、符咒碎片奖励。
- `level_clear_flag`(bool)：是否首次通关该关卡。

**实现方法（模块5内部，不放入接口）：**

- `void on_battle_end_internal(BattleResult result, const std::vector<Enemy*>& defeated)`：若 `result == WIN`，计算奖励。**通过 IInventoryManager\*调用 add_money(int amount)和 add_item(ItemID item_id, int count)发放奖励**。若首次通关，触发结算对话，调用 `on_level_cleared`更新进度。
- `RewardConfig calculate_rewards(int enemy_difficulty)`：根据敌人难度系数计算最终奖励。

------

## 功能6：与战斗引擎、符咒、道具、状态模块的交互

**跨模块指针（通过抽象基类，由外部注入）：**

cpp

```
IBattleEngine* battle_engine;           // 模块1
ITalismanManager* talisman_mgr;         // 模块2
IStatusEffectManager* status_mgr;        // 模块3
IInventoryManager* inventory_mgr;        // 模块4
```

**注入方法（实现类内部，不放入接口）：**

- `set_battle_engine(IBattleEngine* engine)`
- `set_talisman_manager(ITalismanManager* mgr)`
- `set_status_effect_manager(IStatusEffectManager* mgr)`
- `set_inventory_manager(IInventoryManager* mgr)`

**交互方法：**

- `start_battle_for_level(const std::string& level_id)`（已实现为接口方法）：调用 `battle_engine->start_battle(enemy_list)`。
- `on_battle_end(BattleResult result, const std::vector<Enemy*>& defeated)`（已实现为接口方法）：内部调用 `on_battle_end_internal`处理奖励。
- 剧情强制添加状态：通过 `status_mgr->apply_story_status(Entity* target, StatusID id, int duration)`施加（如猴化变形）。
- 发放道具奖励：通过 `inventory_mgr->add_item(ItemID item_id, int count)`发放。

**外部接口依赖（新架构准确签名）：**

| 模块                         | 方法签名                                                     |
| ---------------------------- | ------------------------------------------------------------ |
| 模块1 `IBattleEngine`        | `void start_battle(const std::vector<Enemy*>& enemies)`      |
| 模块2 `ITalismanManager`     | `void add_to_backpack(TalismanID id)`                        |
| 模块3 `IStatusEffectManager` | `void apply_story_status(Entity* target, StatusID id, int duration)` |
| 模块4 `IInventoryManager`    | `bool add_item(ItemID item_id, int count)`/ `void add_money(int amount)` |