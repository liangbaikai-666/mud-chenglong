### 块5：关卡流程与剧情调度（修补版，无场景交互/自由探索）

**共享头文件与前置声明**

- `#include "shared_attributes.h"`（关卡ID、道具ID等常量）
- `#include "character_fwd.h"`（内含 `class Enemy;`前置声明，供 `on_battle_end`使用）

#### 功能1：关卡树结构与进度管理（对应 `LevelManager`类）

**参数（共5项）：**

- `current_layer`(int)：当前所在层，取值1~4（L1/L2/L3/终战）。
- `cleared_levels`(set<string>)：已通关的关卡ID集合。
- `unlocked_levels`(set<string>)：已解锁可进入的关卡ID集合。
- `all_talismans_collected`(bool)：是否集齐全套十二符咒。
- `level_configs`(map<string, LevelConfig>)：关卡配置表（LevelConfig 结构体在 `game_api.h`中定义，含 `id`、`layer`、`prerequisites`、`waves`、`difficulty_coef`、`rewards`）。

**战斗树结构（已确认）：**

- L1：3个关卡
- L2：4个关卡
- L3：5个关卡
- L4：1个关卡（最终战）

**解锁规则（新确认）：**

- L2解锁条件：L1全部3关打通。
- L3解锁条件：L2通过任意2关及以上。
- L4解锁条件：L3通过任意1关及以上。

**实现方法：**

- `void init_progress()`：初始化进度，解锁L1的3个关卡，其余锁定。
- `bool is_level_cleared(const std::string& level_id) const`：返回 `cleared_levels.count(level_id) > 0`。
- `bool can_enter_level(const std::string& level_id) const`：检查是否满足进入条件。若 `level_id`属于L2：检查L1的3个关卡是否全部在 `cleared_levels`中。若 `level_id`属于L3：统计L2中已通关的数量，需≥2。若 `level_id`属于L4：统计L3中已通关的数量，需≥1。同时检查该关卡ID是否在 `unlocked_levels`中。
- `void on_level_cleared(const std::string& level_id)`：将关卡加入 `cleared_levels`，更新 `current_layer`。然后按上述解锁规则检查：若L1全部打通，将L2的4个关卡加入 `unlocked_levels`。若L2通关数≥2，将L3的5个关卡加入 `unlocked_levels`。若L3通关数≥1，将L4的1个关卡加入 `unlocked_levels`。
- `void auto_grant_missing_talismans()`：进入终战前调用，**直接调用模块2的 TalismanManager::add_to_backpack(int id)** 将未收集的符咒加入背包（不经过模块4）。

#### 功能2：关卡进入条件与分支选择（属于 `LevelManager`）

**参数（共2项）：**

- `selected_branch`(string)：当前选择的分支ID。
- `enemy_wave_index`(int)：当前波次索引。

**实现方法：**

- `LevelConfig load_level_config(const std::string& level_id)`：从配置表获取关卡配置。
- `void select_branch(const std::string& branch_id)`：设置 `selected_branch`，校验是否满足进入条件（调用 `can_enter_level`）。
- `vector<Enemy> generate_enemy_wave(int wave_index)`：根据关卡配置和进度难度系数，生成当前波次的敌人列表（使用模块1的 `generate_enemy`逻辑，但由战斗引擎调用）。
- `vector<string> get_available_branches()`：返回当前层所有可选分支ID列表。

#### 功能3：战斗外NPC对话与剧情触发（对应 `DialogueManager`类）

**参数（共3项）：**

- `npc_dialogue_table`(map<string, vector<DialogueNode>>)：NPC对话表，键为NPC名+进度阶段（DialogueNode 在 `game_api.h`中定义）。
- `current_stage`(string)：当前剧情阶段标识（如 "L1_START", "L2_MUSEUM_CLEAR"）。
- `active_npc`(string)：当前正在交互的NPC ID。

**实现方法：**

- `vector<DialogueNode> get_npc_dialogue(const std::string& npc_id) const`：根据 `current_stage`返回对应的对话文本列表。
- `void advance_stage(const std::string& new_stage)`：更新剧情阶段，触发新的对话分支。
- `string on_npc_interact(const std::string& npc_id)`：返回当前阶段该NPC的对话内容，供UI显示。
- `string trigger_guide_hint(GuideType type) const`：小玉引导提示，根据类型返回对应文本（GuideType 枚举在 `game_api.h`中定义）。

#### 功能4：全局游戏状态管理（对应 `GameStateManager`类）

**参数（共1项）：**

- `game_state`(GameState)：枚举值 `{PLAYING, STORY_DEATH, VICTORY, GAME_OVER}`（定义在 `game_api.h`中）。

**实现方法：**

- `GameState get_game_state() const`：返回当前状态。
- `void check_story_death()`：检查是否满足即死条件（如特定剧情节点缺少关键道具/符咒），若满足则设置 `game_state = STORY_DEATH`。
- `void on_victory()`：通关圣主后调用，设置 `game_state = VICTORY`。
- `void on_game_over()`：HP归零且无复活时调用，设置 `game_state = GAME_OVER`。
- **已取消自由探索**：无 `is_free_mode`状态，游戏流程为线性关卡制。

#### 功能5：战斗结算与奖励发放（属于 `LevelManager`）

**参数（共3项）：**

- `battle_result`(BattleResult)：枚举值 `{WIN, LOSE, FLEE}`（定义在 `game_api.h`中）。
- `reward_pool`(RewardConfig)：包含金钱基数、道具掉落率、符咒碎片奖励（结构体在 `game_api.h`中定义）。
- `level_clear_flag`(bool)：是否首次通关该关卡。

**实现方法：**

- `void on_battle_end(BattleResult result, const std::vector<Enemy*>& defeated)`：若 `result == WIN`，计算奖励（金钱+掉落）。**调用模块4的 InventoryManager::add_money(int amount)和 InventoryManager::add_item(const std::string& item_id, int count)发放奖励**。若 `level_clear_flag`为true，触发关卡结算对话。调用 `on_level_cleared`更新进度。
- `RewardConfig calculate_rewards(int enemy_difficulty)`：根据敌人难度系数计算最终奖励数值。

#### 功能6：与战斗引擎、符咒、道具、状态模块的交互（外部接口依赖）

**参数（无独立存储，主要为事件回调）：**

- 各模块引用指针。

**实现方法：**

- `void start_battle_for_level(const std::string& level_id)`：加载关卡配置，生成敌人，调用战斗引擎的 `BattleEngine::start_battle(enemy_list)`。
- `void on_battle_result_received(BattleResult result)`：接收战斗引擎回调，调用 `on_battle_end`。
- `void apply_story_status(const std::string& status_id)`：剧情强制添加状态（如猴化变形），**调用模块3的 StatusEffectManager::add_status(const std::string& id, int duration)**。
- `void grant_item_reward(const std::string& item_id, int count)`：**调用模块4的 InventoryManager::add_item(const std::string& item_id, int count)**。

**外部接口依赖（准确签名，供模块5程序员直接调用）：**

- 模块2 `TalismanManager`：`void add_to_backpack(int id);`
- 模块3 `StatusEffectManager`：`void add_status(const std::string& id, int duration);`
- 模块4 `InventoryManager`：`void add_money(int amount);``bool add_item(const std::string& item_id, int count);`
- 模块1 `BattleEngine`：`void start_battle(const vector<Enemy*>& enemy_list);`