# 模块4 C++ 类设计（新架构版）

基于 `game_api.h`抽象基类架构，以下是模块4（背包与道具系统）的C++类设计。**返回值与实现思路写在注释中，不提供具体实现，由组员自行编写。**

cpp

```cpp
#ifndef INVENTORY_MANAGER_IMPL_H
#define INVENTORY_MANAGER_IMPL_H

#include "game_api.h"       // 包含 IInventoryManager、ItemID 等

#include <map>

// ==================== 道具配置结构体（模块4内部使用） ====================
struct ItemConfig {
    ItemID id;                      // 道具ID
    std::string name;               // 道具名称
    int max_stack;                  // 最大堆叠数量
    int hp_restore;                 // 恢复HP量（0=无）
    int mp_restore;                 // 恢复MP量（0=无）
    int money_value;                // 卖出价格（0=不可卖）
};

// ==================== InventoryManager 类（继承 IInventoryManager） ====================
class InventoryManager : public IInventoryManager {
private:
    // ---- 核心数据 ----
    std::map<ItemID, int> items;             // 背包：道具ID → 数量
    static constexpr int MAX_SLOTS = 10;     // 背包上限10格
    int money;                               // 持有金币数

    // ---- 道具配置表 ----
    std::map<ItemID, ItemConfig> item_configs;  // 道具配置表

    // ---- 跨模块指针（通过抽象基类，由外部注入） ----
    IBattleEngine* battle_mgr;          // 战斗引擎指针
    ITalismanManager* talisman_mgr;     // 符咒管理器指针
    IStatusEffectManager* status_mgr;   // 状态效果管理器指针
    ILevelManager* level_mgr;           // 关卡管理器指针

public:
    // ---- 构造函数 ----
    /**
     * 实现思路：初始化 money = 0，清空 items，
     *          填充 item_configs 配置表（POTION_HP=1, POTION_MP=2 等）
     */
    InventoryManager();

    // ==================== IInventoryManager 接口实现 ====================
    /**
     * 添加道具到背包
     * @param id 道具ID（ItemID enum class）
     * @param count 数量
     * @return void
     * 实现思路：若背包已有该道具，叠加数量（不超过 max_stack）；
     *          若是新道具，检查是否已达 MAX_SLOTS 上限，未达则插入。
     *          超出部分丢弃或提示（由实现决定）
     */
    void add_item(ItemID id, int count) override;

    /**
     * 从背包移除道具
     * @param id 道具ID
     * @param count 数量
     * @return void
     * 实现思路：查找 items[id]，减少对应数量；若减至0则从 map 中 erase
     */
    void remove_item(ItemID id, int count) override;

    /**
     * 查询是否拥有指定道具
     * @param id 道具ID
     * @return bool true=拥有（数量>0），false=未拥有
     * 实现思路：return items.count(id) > 0 && items[id] > 0;
     */
    bool has_item(ItemID id) const override;

    /**
     * 获取指定道具的持有数量
     * @param id 道具ID
     * @return int 持有数量（0=未持有）
     * 实现思路：return items.count(id) > 0 ? items[id] : 0;
     */
    int get_item_count(ItemID id) const override;

    /**
     * 增加金币
     * @param amount 增加量
     * @return void
     * 实现思路：money += amount;
     */
    void add_money(int amount) override;

    /**
     * 消费金币
     * @param amount 消费量
     * @return bool true=足够并扣除，false=不足
     * 实现思路：if money >= amount，扣除并返回 true; else 返回 false
     */
    bool consume_money(int amount) override;

    /**
     * 查询当前金币数
     * @return int 当前金币数
     * 实现思路：return money;
     */
    int get_money() const override;

    /**
     * 使用道具
     * @param id 道具ID
     * @param target 目标实体（通常为玩家）
     * @return void
     * 实现思路：检查 has_item(id)，若拥有则根据配置表调用：
     *          target->heal(hp_restore) 或 target->restore_mp(mp_restore)，
     *          然后 remove_item(id, 1)。
     *          若道具不存在则输出提示
     */
    void use_item(ItemID id, Entity* target) override;

    // ==================== 跨模块指针注入（内部使用，非接口） ====================
    /**
     * 设置战斗引擎指针
     * @param mgr IBattleEngine 实现类指针
     * @return void
     */
    void set_battle_engine(IBattleEngine* mgr);

    /**
     * 设置符咒管理器指针
     * @param mgr ITalismanManager 实现类指针
     * @return void
     */
    void set_talisman_manager(ITalismanManager* mgr);

    /**
     * 设置状态效果管理器指针
     * @param mgr IStatusEffectManager 实现类指针
     * @return void
     */
    void set_status_effect_manager(IStatusEffectManager* mgr);

    /**
     * 设置关卡管理器指针
     * @param mgr ILevelManager 实现类指针
     * @return void
     */
    void set_level_manager(ILevelManager* mgr);

    // ==================== 模块4内部方法（不放入接口） ====================
    /**
     * 获取当前背包格子数
     * @return int 已使用的格子数
     * 实现思路：return items.size();
     */
    int get_used_slots() const;

    /**
     * 查询背包是否已满
     * @return bool true=已满，false=未满
     * 实现思路：return items.size() >= MAX_SLOTS;
     */
    bool is_full() const;

    /**
     * 获取所有背包道具（供存档/UI使用）
     * @return const std::map<ItemID, int>& 背包引用
     */
    const std::map<ItemID, int>& get_all_items() const;
};

#endif // INVENTORY_MANAGER_IMPL_H
```

------

**说明：**

- 头文件引用仅 `#include "game_api.h"`，不引用其他头文件
- 以上仅为 `.h`头文件框架，所有方法只声明、注释返回值意义与实现思路，不含 .cpp 实现代码
- 组员根据注释中的"实现思路"自行编写 `.cpp`文件
- 跨模块调用全部通过抽象基类指针（`IBattleEngine*`/ `ITalismanManager*`/ `IStatusEffectManager*`/ `ILevelManager*`）
- 道具ID全部使用 `ItemID`enum class，不直接用 int
- 符咒相关功能已完全移除，全权交由模块2