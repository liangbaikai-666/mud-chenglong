**模块4 C++类设计**（道具与背包系统）：

cpp

```cpp
#ifndef MODULE4_INVENTORY_H
#define MODULE4_INVENTORY_H

#include "game_api.h"  // 包含共享枚举、前置声明Entity/Player/Enemy、道具ID常量等
#include <vector>
#include <map>
#include <string>

// ==================== 道具基础数据结构（功能1） ====================
struct ItemData {
    std::string id;                 // 道具ID（string，如"POTION_HP"）
    std::string name;               // 中文名，如"生命药水"
    enum { CONSUMABLE, EQUIPMENT, KEY_ITEM, TALISMAN_FRAGMENT } type; // 道具类型
    std::string description;        // 描述
    int max_stack;                  // 最大堆叠数量（非堆叠道具为1）
    int price;                      // 商店价格（0表示不可购买）
    int effect_value;               // 效果数值（如恢复量、攻击力加成等）
    std::string related_status_id;  // 关联的状态ID（消耗品可能施加状态，无则空）
};

// ==================== 背包槽位结构 ====================
struct BackpackSlot {
    std::string item_id;            // 道具ID
    int count;                      // 当前堆叠数量
};

// ==================== InventoryManager 类 ====================
class InventoryManager {
private:
    // ---- 功能1：基础数据 ----
    std::map<std::string, ItemData> all_items;  // 所有道具配置数据

    // ---- 功能2：背包存储 ----
    std::vector<BackpackSlot> backpack;         // 背包槽位数组，上限10
    static const int BACKPACK_CAPACITY = 10;    // 背包容量上限（修补版确认）

    // ---- 功能3：金钱管理 ----
    int money;                                  // 当前持有金钱

public:
    /**
     * 构造函数
     * 实现思路：初始化金钱为0，清空背包，加载道具配置
     */
    InventoryManager() : money(0) {
        backpack.reserve(BACKPACK_CAPACITY);
        load_all_item_data();
    }

    // ---- 功能1：数据加载 ----
    /**
     * 加载所有道具配置数据
     * @return void
     * 实现思路：硬编码或从配置文件读取所有道具数据存入 all_items map。
     *          例如：all_items["POTION_HP"] = {"POTION_HP", "生命药水", CONSUMABLE, "...", 5, 50, 100, ""};
     */
    void load_all_item_data();

    /**
     * 获取道具数据
     * @param id 道具ID（string）
     * @return const ItemData& 对应道具的常量引用
     * 实现思路：返回 all_items[id]
     */
    const ItemData& get_item_data(const std::string& id) const;

    // ---- 功能2：背包管理（核心） ----
    /**
     * 添加道具到背包
     * @param item_id 道具ID（string）
     * @param count 添加数量
     * @return bool true=添加成功，false=背包已满或数量超限
     * 实现思路：先查找背包中是否已有同ID道具且未达max_stack，有则堆叠；
     *          若无空槽（backpack.size() >= BACKPACK_CAPACITY）且无法堆叠，返回false；
     *          否则新增槽位。
     *          【跨模块】供模块2（符咒收集）、模块5（关卡奖励）调用。
     */
    bool add_item(const std::string& item_id, int count);

    /**
     * 从背包移除道具
     * @param item_id 道具ID（string）
     * @param count 移除数量
     * @return bool true=移除成功，false=数量不足或不存在
     * 实现思路：查找背包中对应道具，减少count；若减至0则移除该槽位。
     */
    bool remove_item(const std::string& item_id, int count);

    /**
     * 检查背包是否有足够道具
     * @param item_id 道具ID（string）
     * @param count 需要数量
     * @return bool true=数量充足，false=不足或不存在
     * 实现思路：遍历背包统计该道具总数，与count比较
     */
    bool has_item(const std::string& item_id, int count) const;

    /**
     * 获取背包所有道具
     * @return const std::vector<BackpackSlot>& 背包槽位列表的常量引用
     * 实现思路：返回 backpack
     */
    const std::vector<BackpackSlot>& get_backpack() const;

    // ---- 功能3：金钱管理 ----
    /**
     * 增加金钱
     * @param amount 增加数量
     * @return void
     * 实现思路：money += amount。
     *          【跨模块】供模块5（战斗结算）调用。
     */
    void add_money(int amount);

    /**
     * 消耗金钱
     * @param amount 消耗数量
     * @return bool true=消耗成功，false=金钱不足
     * 实现思路：若 money >= amount，则 money -= amount 并返回true；否则返回false。
     */
    bool consume_money(int amount);

    /**
     * 获取当前金钱
     * @return int 当前金钱数量
     * 实现思路：返回 money
     */
    int get_money() const;

    // ---- 功能4：装备与消耗品使用 ----
    /**
     * 使用道具
     * @param item_id 道具ID（string）
     * @param user 使用者指针（Player*）
     * @return bool true=使用成功，false=使用失败（如不存在、类型不可使用等）
     * 实现思路：检查道具是否存在且为消耗品。根据道具效果：
     *          - 恢复类：调用模块1的 Player::heal(effect_value) 或 consume_mp(-effect_value) 等。
     *          - 状态类：调用模块3的 StatusEffectManager::add_status(user, related_status_id, duration)。
     *          使用后调用 remove_item 减少数量。
     */
    bool use_item(const std::string& item_id, Player* user);

    // ---- 功能5：与战斗引擎交互 ----
    /**
     * 战斗结束补给提示
     * @param defeated 被击败的敌人列表
     * @return void
     * 实现思路：遍历敌人列表，计算可能掉落的道具/金钱，提示玩家（不直接发放，由模块5结算）。
     */
    void on_battle_end_supply_hint(const std::vector<Enemy*>& defeated);

    // ---- 功能6：跨模块接口（供外部调用） ----
    /**
     * 接收模块5的奖励发放
     * @param item_id 道具ID
     * @param count 数量
     * @param money_amount 金钱数量
     * @return void
     * 实现思路：调用 add_item 和 add_money 发放奖励。
     */
    void receive_reward(const std::string& item_id, int count, int money_amount);
};

#endif // MODULE4_INVENTORY_H
```