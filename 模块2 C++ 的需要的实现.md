# 模块2 C++ 类设计（新架构版）

基于 `game_api.h`抽象基类架构，以下是模块2（符咒系统）的C++类设计。**返回值与实现思路写在注释中，不提供具体实现，由组员自行编写。**

cpp

```cpp
#ifndef TALISMAN_MANAGER_IMPL_H
#define TALISMAN_MANAGER_IMPL_H

#include "game_api.h"       // 包含 ITalismanManager、TalismanID、StatusID 等
#include <set>
#include <array>
#include <optional>
#include <vector>

// ==================== 符咒配置结构体（模块2内部使用） ====================
struct TalismanConfig {
    TalismanID id;                    // 符咒ID
    std::string name;                 // 符咒名称
    int cooldown_turns;               // 冷却回合数
    // 效果参数等按需扩展
};

// ==================== TalismanManager 类（继承 ITalismanManager） ====================
class TalismanManager : public ITalismanManager {
private:
    // ---- 核心数据 ----
    std::set<TalismanID> backpack;                         // 背包：已收集的符咒
    std::array<std::optional<TalismanID>, 2> held;         // 手持：最多2个激活的符咒

    // ---- 符咒配置表 ----
    std::vector<TalismanConfig> talisman_configs;          // 12符咒的配置表

    // ---- 跨模块指针（通过抽象基类，由外部注入） ----
    IBattleEngine* battle_mgr;          // 战斗引擎指针
    IStatusEffectManager* status_mgr;   // 状态效果管理器指针

public:
    // ---- 构造函数 ----
    /**
     * 实现思路：初始化 held 两个槽位为 std::nullopt，清空 backpack，
     *          填充 talisman_configs 配置表（12个符咒的ID/名称/冷却等）
     */
    TalismanManager();

    // ==================== ITalismanManager 接口实现 ====================
    /**
     * 将符咒加入背包
     * @param id 符咒ID（TalismanID enum class）
     * @return void
     * 实现思路：backpack.insert(id);
     */
    void add_to_backpack(TalismanID id) override;

    /**
     * 查询背包中是否拥有指定符咒
     * @param id 符咒ID
     * @return bool true=拥有，false=未拥有
     * 实现思路：return backpack.count(id) > 0;
     */
    bool has_talisman(TalismanID id) const override;

    /**
     * 触发符咒效果
     * @param id 符咒ID
     * @param caster 施放者（Entity*，通常为玩家）
     * @return TalismanID 实际触发的符咒ID（可能与传入不同，如手持槽位映射）
     * 实现思路：根据 id 查找对应 TalismanConfig，检查冷却（通过 battle_mgr 查询），
     *          根据符咒类型调用内部效果函数（如 apply_buff/deal_damage 等），
     *          通过 status_mgr 施加相关状态，通过 battle_mgr 启动冷却。
     *          返回实际生效的符咒ID
     */
    TalismanID trigger_talisman(TalismanID id, Entity* caster) override;

    /**
     * 查询符咒是否可触发（冷却/条件检查）
     * @param id 符咒ID
     * @return bool true=可触发，false=不可触发
     * 实现思路：检查 battle_mgr->is_on_cooldown(id) 是否为 false，
     *          且 caster 存活，且符咒在背包或手持中
     */
    bool can_trigger(TalismanID id) const override;

    /**
     * 回合结束时调用（由战斗引擎驱动）
     * @return void
     * 实现思路：遍历手持槽位，对持有符咒调用持续效果逻辑（如龙符咒每回合回血），
     *          通过 battle_mgr->tick_cooldowns() 推进冷却
     */
    void on_turn_end() override;

    /**
     * 战斗开始时调用
     * @return void
     * 实现思路：重置本场战斗相关的临时状态，刷新手持符咒的激活标记
     */
    void on_battle_start() override;

    /**
     * 状态效果施加时回调（由状态模块调用）
     * @param target 目标实体
     * @param status_id 状态ID
     * @param duration 持续回合
     * @return void
     * 实现思路：检查是否持有特定符咒（如猪符咒免疫某些状态），
     *          若匹配则通过 status_mgr 移除或缩短状态持续时间
     */
    void on_status_effect_applied(Entity* target, StatusID status_id, int duration) override;

    /**
     * 请求移除效果（由战斗引擎或状态模块调用）
     * @param caster 施放者
     * @param talisman_id 符咒ID
     * @return void
     * 实现思路：根据 talisman_id 找到对应效果，移除其对 caster 的增益/减益，
     *          若涉及状态则调用 status_mgr->remove_status()
     */
    void request_effect_removal(Entity* caster, TalismanID talisman_id) override;

    // ==================== 跨模块指针注入（内部使用，非接口） ====================
    /**
     * 设置战斗引擎指针
     * @param mgr IBattleEngine 实现类指针
     * @return void
     */
    void set_battle_engine(IBattleEngine* mgr);

    /**
     * 设置状态效果管理器指针
     * @param mgr IStatusEffectManager 实现类指针
     * @return void
     */
    void set_status_effect_manager(IStatusEffectManager* mgr);

    // ==================== 模块2内部方法（不放入接口） ====================
    /**
     * 装备符咒到手持槽位
     * @param id 符咒ID
     * @param slot 槽位索引（0或1）
     * @return bool true=装备成功，false=背包无此符咒或槽位无效
     * 实现思路：检查 backpack 中有 id，然后 held[slot] = id
     */
    bool equip_to_slot(TalismanID id, int slot);

    /**
     * 卸下手持槽位的符咒
     * @param slot 槽位索引（0或1）
     * @return void
     * 实现思路：held[slot] = std::nullopt;
     */
    void unequip_from_slot(int slot);

    /**
     * 获取指定槽位的符咒ID
     * @param slot 槽位索引
     * @return std::optional<TalismanID> 槽位为空则返回 std::nullopt
     */
    std::optional<TalismanID> get_slot(int slot) const;

    /**
     * 获取背包中所有符咒
     * @return const std::set<TalismanID>& 背包引用
     */
    const std::set<TalismanID>& get_backpack() const;
};

#endif // TALISMAN_MANAGER_IMPL_H
```

------

**说明：**

- 以上仅为 `.h`头文件框架，所有方法只声明、注释返回值意义与实现思路，不含 .cpp 实现代码
- 组员根据注释中的"实现思路"自行编写 `.cpp`文件
- 跨模块调用全部通过抽象基类指针（`IBattleEngine*`/ `IStatusEffectManager*`）
- 符咒ID全部使用 `TalismanID`enum class，不直接用 int
- 头文件引用：`game_api.h`