## 模块2 C++ 类设计

基于修补版md和 `game_api.h`，以下是模块2（十二符咒系统）的C++类设计。**返回值与实现思路写在注释中。**

cpp

cpp

下载

复制

```cpp
#ifndef MODULE2_TALISMAN_H
#define MODULE2_TALISMAN_H

#include "game_api.h"  // 包含共享枚举、前置声明Entity/Player/Enemy、TalismanID常量等
#include <vector>
#include <map>
#include <string>

// ==================== 符咒基础数据结构（功能1） ====================
struct TalismanData {
    int id;                     // 符咒数字ID（1~12）
    std::string name;           // 中文名，如"鼠符咒"
    enum { ACTIVE, PASSIVE, BOTH } trigger_type; // 触发类型
    std::string base_effect;    // 效果描述
    int cooldown_turns;         // 冷却回合（常驻为0）
    enum { INSTANT, TURNS, BATTLE, HELD } duration_type; // 持续条件
    int fixed_damage;           // 固定伤害值（无伤害填0）
};

// ==================== TalismanManager 类 ====================
class TalismanManager {
private:
    // ---- 功能1：基础数据 ----
    std::map<int, TalismanData> all_talismans;  // 所有符咒配置数据

    // ---- 功能2：持有与切换 ----
    std::vector<int> backpack;          // 背包内符咒ID列表，上限12
    int handheld[2];                    // 当前手持符咒ID，空槽为-1
    bool is_switching;                  // 是否正在切换
    int switch_target;                  // 待切换的符咒ID

    // ---- 功能3：效果与冷却 ----
    std::map<int, int> active_effects;  // 当前生效效果，键=符咒ID，值=剩余回合（-1常驻，0瞬时，>0持续）
    std::map<int, int> cooldown_timers; // 冷却计时器，键=符咒ID，值=剩余冷却回合

public:
    TalismanManager() : is_switching(false), switch_target(-1) {
        handheld[0] = handheld[1] = -1;
        load_all_talisman_data();
    }

    // ---- 功能1：数据加载 ----
    /**
     * 加载所有符咒配置数据
     * @return void
     * 实现思路：硬编码或从配置文件读取12个符咒的数据，存入 all_talismans map
     *          例如：all_talismans[4] = {4, "兔符咒", PASSIVE, "...", 0, HELD, 0};
     */
    void load_all_talisman_data();

    /**
     * 获取符咒数据
     * @param id 符咒ID（int）
     * @return const TalismanData& 对应符咒的常量引用
     * 实现思路：返回 all_talismans[id]
     */
    const TalismanData& get_talisman_data(int id) const;

    // ---- 功能2：持有与切换管理（与game_api.h声明对齐） ----
    /**
     * 添加符咒到背包
     * @param id 符咒ID（int）
     * @return void
     * 实现思路：若 backpack.size() < 12，插入 id
     */
    void add_to_backpack(int id);

    /**
     * 装备到手持槽
     * @param slot 槽位索引（0或1）
     * @param id 符咒ID（int）
     * @return void
     * 实现思路：handheld[slot] = id；立即调用 apply_passive_effect(id) 应用常驻效果。
     *          【跨模块通知】若 id == TalismanID::RABBIT（4），通过回调或事件通知
     *          战斗引擎（模块1）在本回合增加行动点（由战斗引擎内部限制玩家回合生效）。
     */
    void equip_to_hand(int slot, int id);

    /**
     * 从手持槽卸下
     * @param slot 槽位索引（0或1）
     * @return void
     * 实现思路：清除 handheld[slot]，移除对应常驻效果（调用 remove_passive_effect）
     */
    void unequip_from_hand(int slot);

    /**
     * 发起切换
     * @param new_id 待切换的符咒ID
     * @return void
     * 实现思路：设置 is_switching = true, switch_target = new_id；等待战斗引擎回合结束时调用 finalize_switch
     */
    void initiate_switch(int new_id);

    /**
     * 完成切换
     * @return void
     * 实现思路：更新 handheld 槽位，应用新符咒效果，重置 is_switching 和 switch_target
     */
    void finalize_switch();

    // ---- 功能3：触发与执行（核心） ----
    /**
     * 检查是否可以触发符咒
     * @param id 符咒ID
     * @return bool true=可以触发，false=不可触发（不在手持/冷却中/条件不满足）
     * 实现思路：检查 id 是否在 handheld 中、cooldown_timers[id] == 0、且满足场景条件
     */
    bool can_trigger(int id) const;

    /**
     * 触发符咒效果
     * @param id 符咒ID（int）
     * @param caster 施法者指针（Entity*，Player或Enemy）
     * @return int 造成的伤害值（非伤害类符咒返回0）
     * 实现思路：【跨模块修补①】返回值改为int。
     *          若 can_trigger 通过：
     *          - 主动符咒：调用对应技能函数（如 activate_rat），通过调用功能4的
     *            calculate_spell_damage 获取伤害值，然后设置冷却，返回伤害值。
     *          - 常驻符咒：在 active_effects 中注册（值=-1），并调用模块3的
     *            StatusEffectManager::on_talisman_effect_request 添加Buff。
     *          返回最终伤害值（伤害类）或0（非伤害类）。
     */
    int trigger_talisman(int id, Entity* caster);

    /**
     * 更新效果持续时间
     * @return void
     * 实现思路：遍历 active_effects，将所有值 > 0 的减1；若减至0，移除效果
     */
    void update_durations();

    /**
     * 更新冷却计时器
     * @return void
     * 实现思路：遍历 cooldown_timers，将所有值 > 0 的减1
     */
    void update_cooldowns();

    // ---- 功能4：法术伤害公式 ----
    /**
     * 计算符咒伤害
     * @param talisman_id 符咒ID
     * @param strength 施法者力量（部分符咒可能用到，但固定伤害优先）
     * @param resist 目标是否有 resist_up 状态
     * @return int 最终伤害值
     * 实现思路：直接查询 all_talismans[talisman_id].fixed_damage 作为基础伤害。
     *          若 resist == true，damage = floor(damage / 2)。返回最终值。
     */
    int calculate_spell_damage(int talisman_id, int strength, bool resist) const;

    // ---- 功能5：敌人AI逻辑 ----
    /**
     * 更新敌人AI，决定是否使用符咒
     * @param enemy 敌人指针
     * @param state 当前战斗状态
     * @return void
     * 实现思路：在敌人回合调用，遍历敌人持有的符咒，根据HP阈值等条件决定是否触发。
     *          若触发，调用 trigger_talisman(符咒ID, enemy)
     */
    void enemy_ai_update(Enemy* enemy, GameState state);

    /**
     * 执行敌人符咒
     * @param enemy 敌人指针
     * @param id 符咒ID
     * @return void
     * 实现思路：复用 trigger_talisman 逻辑，caster 设为 enemy
     */
    void execute_enemy_talisman(Enemy* enemy, int id);

    // ---- 功能6：与战斗引擎和状态模块的交互 ----
    /**
     * 战斗开始回调
     * @return void
     * 实现思路：重置所有临时符咒效果（常驻效果保留）
     */
    void on_battle_start();

    /**
     * 回合结束回调（由战斗引擎调用）
     * @return void
     * 实现思路：调用 update_durations() 和 update_cooldowns()
     */
    void on_turn_end();

    /**
     * 状态效果应用回调（供状态模块调用）
     * @param effect_id 状态ID
     * @param duration 持续回合
     * @return void
     * 实现思路：记录符咒产生的状态，用于追踪
     */
    void on_status_effect_applied(int effect_id, int duration);

    /**
     * 请求移除效果（向状态模块发送）
     * @param effect_id 状态ID
     * @return void
     * 实现思路：调用模块3的 StatusEffectManager 对应接口移除状态
     */
    void request_effect_removal(int effect_id);

private:
    // ---- 内部辅助方法 ----
    void apply_passive_effect(int id);    // 应用常驻效果
    void remove_passive_effect(int id);   // 移除常驻效果
};

#endif // MODULE2_TALISMAN_H
```