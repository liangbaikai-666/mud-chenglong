# 《成龙历险记》MUD游戏 —— 开发文档与教学指南

## 第一部分：架构概述与开发规范

### 1.1 架构总览

本项目采用**抽象基类 + 低耦合**架构，核心思想是：

- **game_api.h** 是所有模块的公共契约，只放纯虚接口、`enum class`、结构体和常量。
- 每个模块只依赖 `game_api.h`，**不依赖其他模块的具体实现类**。
- 跨模块调用全部通过**抽象基类指针**进行，由外部（主程序或模块6）注入。

复制

```
game_api.h  ← 所有模块共同依赖
    ↑
模块1~5实现类  ← 各自继承对应接口，通过指针调用其他模块
```

### 1.2 文件结构

复制

```cpp
project/
├── game_api.h              # 共享接口（已定稿v3，不可随意修改）
├── thirdparty/
│   └── nlohmann/json.hpp   # 第三方JSON库（存档用）
├── module1/
│   ├── battle_engine_impl.h # 模块1头文件（已提供框架）
│   └── battle_engine_impl.cpp # 组员编写实现
├── module2/
│   ├── talisman_manager_impl.h
│   └── talisman_manager_impl.cpp
├── module3/
│   ├── status_effect_manager_impl.h
│   └── status_effect_manager_impl.cpp
├── module4/
│   ├── inventory_manager_impl.h
│   └── inventory_manager_impl.cpp
├── module5/
│   ├── module5_impl.h       # 包含三个类
│   └── module5_impl.cpp
└── main.cpp                 # 主程序，负责创建对象、注入指针、启动游戏
```

### 1.3 开发规范（强制）

| 规范             | 说明                                                         |
| ---------------- | ------------------------------------------------------------ |
| **头文件引用**   | 每个模块的实现类 `.h`文件中**只能 #include "game_api.h"**，不得引用其他模块的头文件 |
| **枚举使用**     | 所有ID（符咒/状态/道具）必须使用 `game_api.h`中定义的 `enum class`类型，**禁止直接用 int** |
| **跨模块调用**   | 只能通过接口指针调用，如 `talisman_mgr_->trigger_talisman(...)` |
| **指针注入**     | 各模块提供 `set_xxx_manager(接口*)`方法，由主程序统一注入    |
| **接口不可修改** | `game_api.h`已锁定，组员不得自行修改接口签名                 |

------

## 第二部分：各模块开发指南

> 每个组员拿到对应的 `xxx_impl.h`头文件框架后，只需做两件事：
>
> 1. 新建 `xxx_impl.cpp`，`#include "xxx_impl.h"`
> 2. 按照头文件中注释的“实现思路”逐方法编写函数体

### 模块1：战斗与角色核心（负责人：A）

**头文件：** `battle_engine_impl.h`

**核心类：** `Player`/ `BattleEngine`（继承 `IBattleEngine`）

**开发要点：**

- `Player`类管理角色属性（HP/MP/力量/运气/等级/经验），提供 `get_attr()`/ `modify_attr()`等接口
- `BattleEngine`负责战斗流程：开始战斗 → 玩家行动 → 敌人行动 → 回合结束 → 检查战斗结果
- **跨模块调用示例：**`cppcpp下载复制// 在 BattleEngine::start_battle() 中检查兔符咒if (talisman_mgr_ && talisman_mgr_->has_talisman(TalismanID::RABBIT)) {    action_points_ = 2;}// 在 BattleEngine::execute_action() 中触发符咒if (talisman_mgr_) {    talisman_mgr_->trigger_talisman(static_cast<TalismanID>(action.value), action.actor);}`
- **指针注入：** 主程序调用 `battle_engine->set_talisman_manager(talisman_mgr);`等

### 模块2：符咒系统（负责人：B）

**头文件：** `talisman_manager_impl.h`

**核心类：** `TalismanManager`（继承 `ITalismanManager`）

**开发要点：**

- 背包用 `std::set<TalismanID>`，手持用 `std::array<std::optional<TalismanID>, 2>`
- `trigger_talisman()`是核心方法，根据符咒ID执行对应效果（如龙符咒伤害、兔符咒额外行动点）
- **回调战斗引擎：** 兔符咒触发时调用 `battle_mgr->grant_extra_action()`
- **回调状态模块：** 某些符咒需要施加状态，调用 `status_mgr->add_status(target, StatusID::XXX, duration)`

### 模块3：状态效果系统（负责人：C）

**头文件：** `status_effect_manager_impl.h`

**核心类：** `StatusEffectManager`（继承 `IStatusEffectManager`）

**开发要点：**

- 用 `std::map<StatusID, int>`存储当前激活状态及剩余回合
- `on_turn_end()`中遍历减少持续时间，归零则移除
- **跨模块：** 符咒请求状态效果时，战斗引擎会调用 `on_talisman_effect_request()`，在此方法中根据 `TalismanID`施加对应状态

### 模块4：背包与道具系统（负责人：D）

**头文件：** `inventory_manager_impl.h`

**核心类：** `InventoryManager`（继承 `IInventoryManager`）

**开发要点：**

- 背包用 `std::map<ItemID, int>`，上限10格
- `use_item()`中根据道具配置恢复HP/MP，调用 `target->heal()`/ `target->restore_mp()`（Player类的方法）
- **注意：** 符咒相关功能**完全移除**，全权交由模块2
- 奖励发放（关卡通关后）由模块5调用 `add_item()`/ `add_money()`

### 模块5：关卡/剧情/全局状态（负责人：E）

**头文件：** `module5_impl.h`

**核心类：** `LevelManager`/ `DialogueManager`/ `GameStateManager`

**开发要点：**

- `LevelManager`：管理关卡树L1~L4，通关后解锁后续关卡，调用 `inventory_mgr->add_item()`发奖励
- `DialogueManager`：NPC对话表，根据剧情阶段返回文本
- `GameStateManager`：全局状态查询，战斗失败/胜利时切换状态
- **补全符咒：** `auto_grant_missing_talismans()`中遍历12个 `TalismanID`，调用 `talisman_mgr->add_to_backpack()`