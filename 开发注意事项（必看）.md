# 成龙历险记》MUD游戏 —— 模块开发分发说明

## 一、项目概述

5人协作开发，各模块**低耦合**，仅依赖公共头文件 `game_api.h`中声明的接口，可**并行开发**。

## 二、公共文件（所有人必须包含）

- **game_api.h**：包含共享枚举（GameState/BattleResult/GuideType）、结构体（LevelConfig/RewardConfig/DialogueNode）、前置声明（Entity/Player/Enemy）、常量（TalismanID/StatusID/ItemID）、各模块公开接口类声明。
- ⚠️ **任何人不得自行修改 game_api.h中的接口签名**。实现中若发现必须调整，需全员同步确认。

## 三、各模块职责摘要

| 模块      | 核心类                                                | 职责                                                         |
| --------- | ----------------------------------------------------- | ------------------------------------------------------------ |
| **模块1** | `BattleEngine`, `Player`, `Enemy`                     | 战斗流程控制、回合管理、伤害结算。内部调用模块2的 `trigger_talisman`和模块3的状态查询。 |
| **模块2** | `TalismanManager`                                     | 十二符咒系统：背包/手持/切换、触发效果、伤害计算、敌人AI。`trigger_talisman`返回int伤害值。 |
| **模块3** | `StatusEffectManager`                                 | 状态/Buff/Debuff管理：添加/移除/持续时间更新。状态ID为 `string`类型。 |
| **模块4** | `InventoryManager`                                    | 道具与背包：背包上限 **10**，金钱管理，道具使用（调用模块1/3）。 |
| **模块5** | `LevelManager`, `DialogueManager`, `GameStateManager` | 关卡流程（解锁规则：L2需L1全通、L3需L2通关≥2、L4需L3通关≥1）、NPC对话、全局状态。 |

## 四、关键修补点提醒（实现时请留意）

- 模块2：`trigger_talisman(int id, Entity* caster)`**返回int伤害值**（非伤害类返回0）
- 模块2：装备兔符咒（ID=4）时，需通过回调通知战斗引擎在本回合增加行动点（由引擎内部限制玩家回合）
- 模块3：状态ID统一为 `string`类型；猴化（"MONKEY"）固定2回合，不可移除
- 模块4：背包上限 **10**；金钱归模块4管理
- 模块5：无自由探索模式，为线性关卡制；`on_level_cleared`解锁规则严格按上述条件执行

## 五、并行开发注意事项

1. 先编译 `game_api.h`确保无报错，再开始各自模块的实现。
2. 各模块仅 `#include "game_api.h"`，不要直接 `#include`其他模块的内部头文件。
3. 跨模块调用请严格按 `game_api.h`中声明的函数签名使用。
4. 如有疑问或需要调整设计，及时沟通，不要单方面修改接口。