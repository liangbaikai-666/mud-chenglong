# 模块2：十二符咒系统实现（新架构适配版）

## 继承关系

cpp

```
class TalismanManager : public ITalismanManager { ... };
```

实现 `game_api.h`中定义的 `ITalismanManager`全部纯虚方法，并扩展内部实现。

------

## 共享头文件与前置声明

cpp

```
#include "game_api.h"           // 包含 ITalismanManager、TalismanID、StatusID 等
#include "character_fwd.h"      // 模块1提供，内含 class Entity; class Player; class Enemy; 前置声明
```

> **变更说明**：原设计引用 `shared_attributes.h`（符咒ID常量）和 `character_fwd.h`。新架构下 `TalismanID`已定义为 enum class（1鼠~12猪），不再需要 `shared_attributes.h`中的 int 常量。`game_api.h`已包含 `Entity`前置声明，但 `character_fwd.h`仍用于 `Player`/`Enemy`前置声明。

------

## 功能1：符咒基础数据配置

**参数（共7项/符咒）：**

- `id`(TalismanID)：枚举值，RAT~PIG（1~12）
- `name`(string)：中文名，如 `"鼠符咒"`
- `trigger_type`(enum)：触发类型，`ACTIVE`（主动）、`PASSIVE`（常驻）、`BOTH`（常驻+主动）
- `base_effect`(string)：效果描述文本
- `cooldown_turns`(int)：冷却回合数（常驻为0）
- `duration_type`(enum)：持续条件，`INSTANT`（瞬时）、`TURNS`（持续N回合）、`BATTLE`（本场战斗）、`HELD`（不离手）
- `fixed_damage`(int)：固定伤害值（无伤害填0）

**十二符咒基础数据表（同原设计，略）**

**实现方法：**

- 定义结构体 `TalismanData`封装上述7项参数（id 类型为 `TalismanID`）。
- `load_all_talisman_data()`：硬编码或从配置文件读取，存入 `std::map<TalismanID, TalismanData> all_talismans`。
- `get_talisman_data(TalismanID id)`：返回对应符咒的常量引用。

------

## 功能2：符咒持有与切换管理

**参数（共4项）：**

- `backpack`(std::set<TalismanID>)：背包内符咒ID集合，上限12（使用 set 自动去重）。
- `handheld`(std::array<std::optional<TalismanID>, 2>)：当前手持符咒ID，空槽为 `std::nullopt`。
- `is_switching`(bool)：是否正在切换符咒（消耗1行动回合）。
- `switch_target`(TalismanID)：待切换入手的符咒ID。

**实现方法：**

- `void add_to_backpack(TalismanID id) override`：若背包未满（size < 12），插入 `backpack`。
- `bool has_talisman(TalismanID id) const override`：返回 `backpack.count(id) > 0`。
- `equip_to_hand(int slot, TalismanID id)`：将手持槽 `slot`（0或1）设为 `id`，并立即应用常驻效果（调用功能3的 `apply_passive_effect`）。**若装备兔符咒（id == TalismanID::RABBIT），通知战斗引擎（通过 IBattleEngine\*回调）本战斗回合行动点+1（由战斗引擎内部限制玩家回合生效）。**
- `unequip_from_hand(int slot)`：清除槽位，移除对应常驻效果。
- `initiate_switch(TalismanID new_id)`：设置 `is_switching = true`和 `switch_target`，等待战斗引擎在回合结束时调用 `finalize_switch()`。
- `finalize_switch()`：完成切换，更新 `handheld`，应用新效果，重置标志。
- `on_talisman_lost(TalismanID id)`：从 `backpack`和 `handheld`中移除该ID，并清除所有相关效果。

------

## 功能3：符咒效果触发与执行

**参数（共3项）：**

- `active_effects`(std::map<TalismanID, int>)：当前生效的符咒效果，键为符咒ID，值为剩余持续回合（-1为常驻，0为瞬时，>0为剩余回合）。
- `cooldown_timers`(std::map<TalismanID, int>)：符咒冷却计时器，键为符咒ID，值为剩余冷却回合。
- `caster`(Entity*)：触发者指针。

**实现方法：**

- `bool can_trigger(TalismanID id) const override`：检查符咒是否在手持中、冷却是否为0、是否满足场景条件。
- `TalismanID trigger_talisman(TalismanID id, Entity* caster) override`：返回触发的符咒ID（即传入的 id）。若 `can_trigger`通过，根据 `trigger_type`执行：主动：调用对应符咒的技能函数（如 `activate_rat(id, caster)`），然后设置冷却。常驻：在 `active_effects`中注册效果（值为 -1），并调用状态模块添加Buff（通过 `IStatusEffectManager*`）。
- `void on_turn_end() override`：每回合减少 `active_effects`中大于0的值的持续时间，到期自动移除效果；`-1`不受影响。同时减少 `cooldown_timers`中所有正值。
- `void on_battle_start() override`：战斗开始时重置所有临时符咒效果（常驻效果保留）。

------

## 功能4：法术伤害公式实现

**参数（共2项）：**

- `caster_strength`(int)：施法者的力量值（用于部分符咒计算，但固定伤害优先）。
- `target_has_resist`(bool)：目标是否拥有 `StatusID::RESIST_UP`状态（来自模块3）。

**实现方法：**

- `calculate_spell_damage(TalismanID talisman_id, int strength, bool resist)`：直接查询 `all_talismans[talisman_id].fixed_damage`作为基础伤害。若 `resist == true`，`damage = floor(damage / 2)`。返回最终伤害值（int）。**（与 game_api.h 中模块1调用逻辑一致）**

------

## 功能5：敌人持有符咒AI逻辑

**参数（共2项）：**

- `enemy_talismans`(std::vector<TalismanID>)：敌人持有的符咒ID列表。
- `ai_trigger_threshold`(float)：主动符咒使用阈值（如HP低于50%时可能使用）。

**实现方法：**

- `enemy_ai_update(Enemy* enemy, BattleState state)`：在敌人回合调用，遍历 `enemy_talismans`，根据符咒ID和当前战况决定是否触发。例如：若持有龙符咒（TalismanID::DRAGON）且冷却完毕，且玩家HP较低，则调用 `trigger_talisman(TalismanID::DRAGON, enemy)`。
- `execute_enemy_talisman(Enemy* enemy, TalismanID id)`：复用功能3的触发逻辑，但将 `caster`设为敌人。

------

## 功能6：与战斗引擎和状态效果模块的交互

**跨模块指针（通过抽象基类，由外部注入）：**

cpp

```
IBattleEngine* battle_engine;           // 模块1（通知行动点变化）
IStatusEffectManager* status_mgr;       // 模块3（添加/移除状态）
```

**注入方法（实现类内部，不放入 ITalismanManager 接口）：**

- `set_battle_engine(IBattleEngine* engine)`
- `set_status_effect_manager(IStatusEffectManager* mgr)`

**实现 ITalismanManager 接口的方法：**

- `void on_status_effect_applied(Entity* target, StatusID id, int duration) override`：供状态模块回调，记录符咒产生的状态。
- `void request_effect_removal(Entity* target, StatusID id) override`：向状态模块发送移除请求。
- **移除 on_talisman_triggered**（新架构中不存在，避免误用）。