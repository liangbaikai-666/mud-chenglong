#### 模块2：十二符咒系统实现（修补版）

**共享头文件与前置声明**

- `#include "shared_attributes.h"`（符咒ID常量，如 `TalismanID::RABBIT`代表4）
- `#include "character_fwd.h"`（内含 `class Entity;`前置声明，供 `trigger_talisman`使用）

#### 功能1：符咒基础数据配置

**参数（共7项/符咒）：**

- `id`(int)：数字ID，1~12（建议使用 `shared_attributes.h`中的常量）
- `name`(string)：中文名，如 `"鼠符咒"`
- `trigger_type`(enum)：触发类型，`ACTIVE`（主动）、`PASSIVE`（常驻）、`BOTH`（常驻+主动）
- `base_effect`(string)：效果描述文本
- `cooldown_turns`(int)：冷却回合数（常驻为0）
- `duration_type`(enum)：持续条件，`INSTANT`（瞬时）、`TURNS`（持续N回合）、`BATTLE`（本场战斗）、`HELD`（不离手）
- `fixed_damage`(int)：固定伤害值（无伤害填0）

**十二符咒基础数据表（第一版，同原表，略）**

**实现方法：**

- 定义结构体 `TalismanData`封装上述7项参数。
- `load_all_talisman_data()`：硬编码或从配置文件读取，存入 `std::map<int, TalismanData> all_talismans`。
- `get_talisman_data(int id)`：返回对应符咒的常量引用。

#### 功能2：符咒持有与切换管理

**参数（共4项）：**

- `backpack`(vector<int>)：背包内符咒ID列表，上限12。
- `handheld`(array<int, 2>)：当前手持符咒ID，空槽为 `-1`。
- `is_switching`(bool)：是否正在切换符咒（消耗1行动回合）。
- `switch_target`(int)：待切换入手的符咒ID。

**实现方法：**

- `add_to_backpack(int id)`：若背包未满，插入 `backpack`。
- `equip_to_hand(int slot, int id)`：将手持槽 `slot`（0或1）设为 `id`，并立即应用常驻效果（调用功能3的 `apply_passive_effect`）。**若装备兔符咒（id==TalismanID::RABBIT），通知战斗引擎（通过回调）本战斗回合行动点+1（由战斗引擎内部限制玩家回合生效）。**
- `unequip_from_hand(int slot)`：清除槽位，移除对应常驻效果。
- `initiate_switch(int new_id)`：设置 `is_switching = true`和 `switch_target`，等待战斗引擎在回合结束时调用 `finalize_switch()`。
- `finalize_switch()`：完成切换，更新 `handheld`，应用新效果，重置标志。
- `on_talisman_lost(int id)`：从 `backpack`和 `handheld`中移除该ID，并清除所有相关效果。

#### 功能3：符咒效果触发与执行

**参数（共3项）：**

- `active_effects`(map<int, int>)：当前生效的符咒效果，键为符咒ID，值为剩余持续回合（-1为常驻，0为瞬时，>0为剩余回合）。
- `cooldown_timers`(map<int, int>)：符咒冷却计时器，键为符咒ID，值为剩余冷却回合。
- `caster`(Entity*)：触发者指针（前置声明自 `character_fwd.h`）。

**实现方法：**

- `can_trigger(int id)`：检查符咒是否在手持中、冷却是否为0、是否满足场景条件。
- `int trigger_talisman(int id, Entity* caster)`：**返回值改为 int（伤害值）**。若 `can_trigger`通过，根据 `trigger_type`执行：主动：调用对应符咒的技能函数（如 `activate_rat(id, caster)`），计算伤害（调用功能4），然后设置冷却。返回造成的伤害值（若为伤害类符咒）或0（非伤害类）。常驻：在 `active_effects`中注册效果（值为 `-1`），并调用状态模块添加Buff（通过外部接口）。
- `update_durations()`：每回合减少 `active_effects`中大于0的值的持续时间，到期自动移除效果；`-1`不受影响。
- `update_cooldowns()`：每玩家回合减少 `cooldown_timers`中所有正值。

#### 功能4：法术伤害公式实现

**参数（共2项）：**

- `caster_strength`(int)：施法者的力量值（用于部分符咒计算，但固定伤害优先）。
- `target_has_resist`(bool)：目标是否拥有 `resist_up`状态（来自模块3）。

**实现方法：**

- `calculate_spell_damage(int talisman_id, int strength, bool resist)`：直接查询 `all_talismans[talisman_id].fixed_damage`作为基础伤害。若 `resist == true`，`damage = floor(damage / 2)`。返回最终伤害值（int）。**（与 game_api.h中模块1调用逻辑一致）**

#### 功能5：敌人持有符咒AI逻辑

**参数（共2项）：**

- `enemy_talismans`(vector<int>)：敌人持有的符咒ID列表。
- `ai_trigger_threshold`(float)：主动符咒使用阈值（如HP低于50%时可能使用）。

**实现方法：**

- `enemy_ai_update(Enemy* enemy, BattleState state)`：在敌人回合调用，遍历 `enemy_talismans`，根据符咒ID和当前战况决定是否触发。例如：若持有龙符咒且冷却完毕，且玩家HP较低，则调用 `trigger_talisman(5, enemy)`（注意参数类型为 `Entity*`，`Enemy`继承自 `Entity`）。
- `execute_enemy_talisman(Enemy* enemy, int id)`：复用功能3的触发逻辑，但将 `caster`设为敌人。

#### 功能6：与战斗引擎和状态效果模块的交互

**参数（无独立存储，主要为事件钩子）：**

- 回调函数指针或观察者模式接口。

**实现方法：**

- `on_battle_start()`：注册到战斗引擎，战斗开始时重置所有临时符咒效果（常驻效果保留）。
- `on_turn_end()`：注册到战斗引擎，每回合结束时调用 `update_durations()`和 `update_cooldowns()`。
- `on_status_effect_applied(int effect_id, int duration)`：供状态模块回调，记录符咒产生的状态。
- `request_effect_removal(int effect_id)`：向状态模块发送移除请求。
- **移除 on_talisman_triggered**（原修补方案要求标记为预留，此处不实现，避免误用）。